function e(e){if(e.category===`cd`)return`- Avoid a standalone Bash \`cd\` command — its working-directory change ends with the call; use ${e.nativeTool}, or chain \`cd <dir> && <cmd>\` to anchor a same-call command.`;let t=(e.observedCommands&&e.observedCommands.length>0?e.observedCommands:[e.category]).map(e=>`Bash \`${e}\``).join(` or `);return`- Use the native ${e.nativeTool} tool instead of ${t}.`}function t(t){let n=[{category:`grep`,nativeTool:`Grep`},{category:`find`,nativeTool:`Glob`},{category:`cat`,nativeTool:`Read`}],r=new Set;return[`## Prefer native tools and path-safe shell usage`,``,`For avoidable shell patterns, choose native tools or path-safe alternatives before Bash.`,`Keep file work structured and permission-aware. Apply these examples; category names are generic when an observed alias is unavailable:`,``,...(t.length>0?t:n).filter(e=>r.has(e.category)?!1:(r.add(e.category),!0)).map(e)].join(`
`)}function n(t){return e(t).replace(/^- /,``)}export{n,t};