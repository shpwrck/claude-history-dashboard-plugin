{
  "mcpServers": {
    "claude-history-dashboard": {
      "type": "stdio",
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/scripts/mcp-shim.mjs"],
      "env": {
        "CHD_PORT": "5173",
        "CHD_HOST": "127.0.0.1"
      },
      "description": "Read/control tools for the Claude History Dashboard local server. Exposes dashboard_status, get_recommendations, and top_frictions. The dashboard server must already be running (via plugin-ctl.mjs start or the commands/dashboard.md command); this shim is stateless and does not start the server."
    }
  }
}
