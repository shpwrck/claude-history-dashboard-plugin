# Per-view actionability detail

> 22 views, 555 data points, 83 hard dead-ends (actionability:none). Run 2026-06-03.

## Recommendations — **MIXED**

_Surface actionable, ranked findings from every dashboard signal so the user knows exactly what to change — config file, behaviour, or model — to cut cost, improve context health, reduce errors, and tighten safety._

Top fixes: - Wire evidence chips in secondary recommendation cards to SessionIdLink (Recommendations.tsx:1647-1657): evidence items that are 8-char session-id prefixes render as plain grey Labels with no click handler, yet session drill-in is the single highest-value action the user could take — they see 'abc12345, $3.40, bash' and cannot open that session without manually searching for it. Detect session-id tokens and swap in SessionIdLink with onOpenSession.
- Add a CopyButton for the settings.json / CLAUDE.md fix snippet in the CheaperModelCard footer (Recommendations.tsx:1143-1150) and for the /insights command hint (Recommendations.tsx:968): both describe high-impact actions ('pin Haiku in settings.json', 'run /insights') entirely in prose, with no clipboard affordance. The user must manually type the command or snippet. A single CopyButton each converts these from read-only advice into one-click actions.
- Wire the SeveritySummaryBar segments to setSeverityFilter (Recommendations.tsx:536-553): the colored proportion bar visually implies click-to-filter (it has title tooltips, it looks like a navigation element) but has no onClick. Clicking a segment should apply the same filter as the severity chips below — this removes the false-affordance and doubles the surface area for filtering, which is especially important when there are many recommendations.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Budget Gauge — 5-hour session utilization bar | cost | high | Refresh button onClick (BudgetGauge.tsx:264) re-fetches live usage; the utilization bar itself has no click handler | keep — already actionable; the refresh button closes the 'am I near my cap?' question in real time |
| Budget Gauge — Weekly cap utilization bar | cost | high | Refresh button only; the weekly bar itself has no click handler | keep — already actionable via the Refresh button |
| Budget Gauge — Weekly pace projection ('on pace for X% by reset time') | cost | medium | NONE — dead-end: pure text projection, no click target | Add a link/button that navigates to the Cost view so the user can immediately inspect which sessions are driving projected overage |
| Budget Gauge — 'Binding limit right now: X' label | orientation | low | NONE — dead-end | Cut or collapse into the bar labels; it adds no action the user can take from this surface |
| Budget Gauge — Overage active warning | cost | medium | NONE — dead-end | Add a navigate-to-cost button so the user can immediately drill into what is burning the overage |
| Live Session widget (server-only) | context-health | medium | No navigation handler wired from Recommendations.tsx — LiveSession renders its own Card but has no onNavigate/onOpenSession prop passed to it | Pass onNavigate so the live-session card can link to the Sessions view for the active session; currently the retry-storm and re-read-loop 'why' badges are displayed but not drillable |
| Weekly delta — Cost tile (prev → now + % change arrow) | cost | high | onClick on isClickable Card (line 631) toggles drilldown expand/collapse | keep — already actionable |
| Weekly delta — API errors tile (prev → now + % change arrow) | success-rate | high | onClick on isClickable Card (line 631) toggles drilldown | keep — already actionable |
| Weekly delta — Retry storms tile (prev → now + % change arrow) | success-rate | high | onClick on isClickable Card (line 631) toggles drilldown | keep — already actionable |
| Weekly drilldown — dimension breakdown table rows (project / model / day) | cost | medium | FALSE AFFORDANCE — rows have a barPct inline bar suggesting interactive heat, but no click handler; dimension=project rows show project names with no link to the Projects view | Make project-dimension rows clickable to navigate to the Projects view filtered to that project; model-dimension rows could link to the Tokens view filtered to that model |
| Weekly drilldown — contributing sessions list (SessionIdLink + project name + value delta) | cost | high | SessionIdLink onClick (SessionIdLink.tsx:59) calls onOpen(sessionId) which navigates to Sessions view focused on that session | keep — already actionable |
| Weekly drilldown — 'Open Sessions →' button | discovery | medium | onClick (line 889) calls onNavigate('sessions') — navigates but loses drilldown context | Pass the contributing session IDs as a filter hint to the Sessions view so the user lands on the relevant subset instead of the unfiltered list |
| Top Action Hero — severity badge (Critical/Warning/Info) | orientation | low | NONE — dead-end: decorative label | Wire to severity filter so clicking it filters the secondary list to matching severity (same as the filter chips below) |
| Top Action Hero — category badge (Cost/Context/Workflow/Safety/Reliability/Activity) | orientation | low | NONE — dead-end: decorative label | Wire to category filter on the secondary list (filter chips below only cover severity, not category) |
| Top Action Hero — 'Unattended' badge | safety | medium | NONE — dead-end: tooltip explains it but no navigation | Add onClick navigating to Permissions view so user can immediately inspect the unattended session's entrypoint and permissions |
| Top Action Hero — estimated savings dollar amount | cost | medium | NONE — dead-end | keep context — the adjacent 'view →' button provides the drill-in; the number itself needs no extra affordance |
| Top Action Hero — 'View →' navigation button (e.g. 'Cost →') | discovery | high | onClick (line 1596) navigates to the relevant dashboard view | keep — already actionable |
| Top Action Hero — Fix snippet 'Copy fix' button | workflow-hygiene | high | CopyButton onClick (Recommendations.tsx:1450) copies the fix snippet to clipboard | keep — already actionable; clipboard copy is the right affordance for a config snippet |
| Top Action Hero — Fix target label badge (e.g. 'settings.json', 'CLAUDE.md') | workflow-hygiene | low | NONE — dead-end: decorative label showing the target file | Low priority — context is already provided by the snippet; no change needed |
| Severity summary bar — colored proportion segments (Critical / Warning / Info) | orientation | medium | FALSE AFFORDANCE — the bar looks like a clickable filter (proportion blocks, hover via title tooltip) but has no onClick handler | Wire each segment's onClick to setSeverityFilter(sev) — same as the filter chips below, but discoverable directly from the bar |
| Severity summary bar — text counts (Critical: N, Warning: N, Info: N, Total: N) | orientation | low | NONE — dead-end | Wire onClick to setSeverityFilter or cut the legend (counts are already visible on the bar segments) |
| KPI tile — 'N Recommendations' count | orientation | low | NONE — dead-end | Cut — the list below is the actionable form; the count tile adds no action |
| KPI tile — 'Recoverable Spend' dollar amount | cost | medium | NONE — dead-end | Add onClick navigating to the Cost view so the user can inspect the sessions behind the recoverable figure |
| Compaction Risk Card — '% of sessions running hot' headline + session count | context-health | medium | NONE — dead-end on the stat itself; adjacent button provides navigation | keep context — the 'Context Health →' button is the right affordance; the stat itself needs no extra click |
| Compaction Risk Card — 'Most common avoidable cause' suggestion | context-health | medium | NONE — dead-end: plain text suggestion | Add a CopyButton alongside the suggestion so the user can paste it into their CLAUDE.md immediately |
| Compaction Risk Card — 'Context Health →' button | context-health | high | onClick (line 1749) navigates to the Context Health view | keep — already actionable |
| Secondary recommendation cards — severity + category badges | orientation | low | NONE — dead-end: decorative labels | Wire severity badge onClick to toggle severityFilter; wire category badge to a category filter (currently missing) |
| Secondary recommendation cards — evidence chips (session id, tool name, file name, etc.) | discovery | medium | FALSE AFFORDANCE — isCompact Label styling visually resembles interactive chips but have no onClick; session-id evidence items are not wired to openSession | Detect session-id evidence items (8-char hex prefix) and render them as SessionIdLink instead of a plain Label so they navigate to that session; other evidence (file names, tool names) can stay as plain chips |
| Secondary recommendation cards — estimated savings dollar amount | cost | low | NONE — dead-end | keep context — the adjacent 'view →' button provides the drill-in |
| Secondary recommendation cards — 'View →' navigation button | discovery | high | onClick (line 1672) navigates to the relevant dashboard view | keep — already actionable |
| Secondary recommendation cards — Fix snippet 'Copy fix' button | workflow-hygiene | high | CopyButton onClick (Recommendations.tsx:1450) writes snippet to clipboard | keep — already actionable |
| Severity filter chips (Critical / Warning / Info toggle) | discovery | high | onClick (line 435) toggles severity filter on the secondary rec list | keep — already actionable; add a matching category filter for parity |
| Config Hygiene — settings.json Valid/Error/Warning badge | workflow-hygiene | medium | NONE — dead-end: shows pass/fail label but no navigation to fix the file | Show the file path as a copyable element (already shown at line 125-126); when there are errors, surface the 'python3 -m json.tool <path>' repair command with a CopyButton |
| Config Hygiene — settings finding card (severity + path + line + message + excerpt) | workflow-hygiene | high | NONE — dead-end: repair command is plain text not copyable | Wrap the python3 repair command (ConfigHygiene.tsx:164-170) in a CopyButton so the user can run it immediately; add a CopyButton for the path at line 125-126 |
| Config Hygiene — unused resource row (skill/subagent/MCP/plugin): resource ID + last-seen + lifetime count | workflow-hygiene | medium | NONE — dead-end: displays resource name, scope, last-used age, and lifetime count but no action to prune, disable, or navigate to the resource's settings file | Add a CopyButton with the remove/prune command (e.g. for MCP: the JSON snippet to remove from settings) or at minimum a 'Copy name' button so the user can grep their settings.json; the lifetime count alone is vanity |
| Cheaper-model routing — headline savings sentence and downgradable % stat | cost | medium | NONE — dead-end | Keep the headline; add a CopyButton with the settings.json model snippet (already shown at the bottom of the fix blocks on related recs) so the user can act immediately |
| Cheaper-model routing — turn distribution bar (Trivial/Moderate/Complex segments) | cost | low | NONE — dead-end | Low priority; add title tooltips already present; no change needed |
| Cheaper-model routing — model stat cards (Haiku turns, Sonnet turns, Est. savings observed) | cost | low | NONE — dead-end | Haiku/Sonnet turn counts: wire onClick to Tokens view filtered to those models; Est. savings is vanity — keep only if a CopyButton for the fix snippet is present |
| Cheaper-model routing — recent trivial turn rows (prompt summary + feature chips + recommended model label) | cost | low | NONE — dead-end: the prompt summary and feature chips are purely informational; the session that produced this turn is not linked | Expose the sessionId on TurnRec and render it as a SessionIdLink so the user can inspect the full session behind each trivial turn |
| Cheaper-model routing — footer guidance text ('See the model key in settings.json...') | cost | high | NONE — dead-end: the action (edit settings.json) is described in prose but not executable from the UI | Add a CopyButton with { "model": "claude-haiku-4-5" } snippet directly in this card so the user can act without leaving the view |
| Insights — 'Run /insights in Claude Code' hint card | workflow-hygiene | medium | NONE — dead-end: the command is in a <code> inline, not a CopyButton | Wrap /insights in a CopyButton so the user can copy and paste it into their terminal immediately |
| Insights — CLAUDE.md suggestion code blocks with per-item CopyButton | workflow-hygiene | high | CopyButton onClick (line 1450) writes snippet to clipboard; 'Copy All' writes all items joined | keep — already actionable |
| Insights — Features to Try cards: title, oneliner, why, example code with CopyButton | workflow-hygiene | high | CopyButton onClick copies the example code snippet | keep — already actionable |
| Insights — Usage Patterns: title, summary, detail, copyable prompt with CopyButton | workflow-hygiene | medium | CopyButton onClick copies the prompt when present; patterns without copyablePrompt are dead-end | Patterns without a copyablePrompt should at minimum surface a CopyButton for the pattern title or a template the user can adapt |

## BudgetGauge (Plan Usage Card) — **MIXED**

_Show the user how close they are to their Claude subscription limits (5-hour session and weekly cap) so they can pace or stop work before hitting a rate-limit wall._

Top fixes: - Wire the 'limit reached' badge and the Overage active notice to a drill-in action (navigate to Sessions view filtered to the current window, or scroll to the Recommendations panel) — these are the two highest-stakes signals in the card and currently both dead-ends.
- Add a PatternFly ClipboardCopy (CopyButton) to the 'claude auth' CodeBlock in the degraded state — the entire purpose of that state is to get the user to run one command, and copy-friction is the only thing standing between them and recovery.
- Convert the weekly pace projection's color-coded percentage into a link to the top-cost sessions for the window when projected >= 80% — the projection warns but gives no path to act.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| 5-hour session utilization bar (%, color variant) | cost | medium | NONE — dead-end | Add an onClick that navigates to the Sessions view filtered to the current 5-hour window, so the user can see which sessions are eating the budget and decide which work to defer. |
| 5-hour reset time + countdown (e.g. 'resets Fri 14:30 (in 2h 14m)') | cost | medium | NONE — dead-end | Cheapest fix: copy-to-clipboard button on the reset time so the user can paste it into a calendar block or reminder. Or link to the Sessions view pre-filtered to show sessions in the current 5-hour window. |
| 'limit reached' inline badge on 5-hour bar (shown when status==='rejected') | cost | high | NONE — dead-end | Wire to the Recommendations view (or scroll-to the recs panel) so the user immediately sees which sessions/tools drove them over — the binding-limit detector in the rec engine already knows this. |
| Weekly cap utilization bar (%, color variant) | cost | medium | NONE — dead-end | Same as 5-hour bar: onClick navigate to Sessions view filtered to current 7-day window. |
| Weekly cap reset time + countdown (e.g. 'resets Sun 00:00 (in 5d 3h)') | orientation | low | NONE — dead-end | Copy-to-clipboard button on the reset datetime is the cheapest addition; otherwise keep as contextual support data for the bar above it. |
| 'limit reached' badge on Weekly cap bar (status==='rejected') | cost | high | NONE — dead-end | Same as 5-hour rejected badge: link to Recommendations panel or top-cost sessions for the week so the user can cut spend. |
| Weekly pace projection ('X% now, on pace for Y% by reset') | cost | medium | NONE — dead-end | When projected >= 100%, surface a direct link to the cost-breakdown view or the most expensive session in the window so the user knows where to cut. Currently the red/yellow color is the only signal — it tells you a problem exists but not how to fix it. |
| Binding limit label ('Binding limit right now: the 5-hour session window') | orientation | low | NONE — dead-end | This is useful context for interpreting the bars above, but purely informational. Either cut it (the bars already show which window is more consumed) or link it to the relevant bar's drill-in. |
| Overage active notice + % of overage allowance used | cost | high | NONE — dead-end | This is the highest-stakes datum in the component. Wire an immediate link to top-cost sessions for the billing period, or to the Recommendations panel, so the user can act on it. 'Usage beyond the plan limit is being billed' with nowhere to go is a missed conversion. |
| Refresh button (normal state — re-fetches /api/usage) | cost | high | onClick at line 269 — triggers fetchUsage() re-poll | keep — already actionable |
| Refresh button (degraded/unavailable state) | cost | high | onClick at line 229 — triggers fetchUsage() re-poll | keep — already actionable |
| 'claude auth' command in CodeBlock (degraded state — auth-failed or not-logged-in) | workflow-hygiene | high | NONE — dead-end (CodeBlock is display-only; no CopyButton wired) | Add a PatternFly CopyButton (ClipboardCopy component) so the user can paste 'claude auth' directly into their terminal. Without it the user must manually transcribe the command from the card — friction on the single path to fixing the broken auth state. |
| Card subtitle 'Live from your Claude subscription · am I about to hit my cap?' | orientation | none | NONE — dead-end | Cut it. 'Live from your Claude subscription' is redundant once the user sees live numbers. The rhetorical question 'am I about to hit my cap?' adds no information. The freed space can hold a link to the Sessions drill-in instead. |

## Search — **MIXED**

_Let users find past prompts by keyword and jump into the full conversation in the Sessions view._

Top fixes: - Fix the false affordance on the Card: add onClick={() => onOpenSession(entry.sessionId)} to the Card element (src/components/SearchView.tsx:134). The card is styled and aria-marked as clickable but only the inner button is wired — this is the most jarring gap.
- Render entry.title as a card heading when present (src/types.ts:9 has the field; it is never used in SearchView). Users scanning results need a human label ('Fix SPA boundary CI job') not just a truncated raw prompt and a path.
- Make the project name chip clickable to set the projectFilter dropdown (src/components/SearchView.tsx:160). Clicking a project label to scope results is an expected affordance and requires one line of state wiring.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Search input (keyword box) | discovery | high | onChange sets query state, onClear resets it — fully wired instrument | keep — already actionable |
| Project filter dropdown | discovery | high | onChange sets projectFilter state — fully wired instrument | keep — already actionable |
| Result count badge ('N results' / 'showing first 100') | orientation | low | NONE — dead-end plain text | When count hits the 100-cap, add a 'Narrow your search or add a project filter' hint inline, or make the badge a link that filters to the selected project view. The cap itself is silent signal that a user's query is too broad — surfacing that nudge converts a dead-end into an action. |
| Entry timestamp (date/time per result card) | orientation | low | NONE — dead-end plain text | Wire the timestamp to the same onOpenSession(entry.sessionId) as the 'Open in Sessions' button, or remove it in favour of a relative-time label ('3 days ago') that is both shorter and context-setting. Neither requires a new data source. |
| Entry project name (shortened path per result card) | discovery | low | NONE — dead-end plain text, not clickable | Make the project label a clickable chip: clicking it sets the projectFilter dropdown to that project (already a controlled value), narrowing results to that project instantly. One line of onChange wiring, no new components. |
| Result card container (isClickable + cursor:pointer styling) | discovery | none | FALSE AFFORDANCE — isClickable PF prop and cursor:pointer style signal the whole card is clickable, but only the inner Button at line 165 has an onClick; clicking card chrome/whitespace does nothing | Add onClick={() => onOpenSession(entry.sessionId)} to the Card element (or CardBody), matching what the inner button does. Then the false affordance becomes real. Cost: one attribute. |
| 'Open in Sessions' button per result card | discovery | high | onClick fires onOpenSession → App.tsx:253-259 sets focusSessionId + navigates to 'sessions' view | keep — already actionable |
| Prompt text / highlighted match snippet per result card | discovery | medium | NONE — dead-end read-only text | The snippet is the reason the user is here — it passes context — but it is truncated at 300 chars with no way to see more without leaving the view. Add an expand toggle ('Show more') that reveals the full prompt inline, or rely on the 'Open in Sessions' button already present. The truncation dead-end is the only real friction; expand-toggle is the cheapest fix. |
| Prompt truncation indicator ('...' at 300 chars) | discovery | low | NONE — dead-end ellipsis with no expand control | Replace static ellipsis with an inline 'Show full prompt' toggle button. The full text is already available in entry.display; this is pure UI state — no fetch needed. |
| Session title (entry.title field) | discovery | none | NONE — data exists in HistoryEntry.title (populated by deriveEntriesFromTranscript) but is never rendered in any card | Render entry.title (when present) as a bold heading above the prompt snippet, replacing the bare timestamp-only header. The title gives users a human-readable anchor ('Fix SPA boundary CI job') without needing to read the raw prompt text. |

## patterns — **MIXED**

_Surface session shape clusters and per-habit outcome correlations so users can identify which working patterns cost less, error less, and compress less — and change the habits that hurt._

Top fixes: - Wire setTag(sessionId, 'good'|'bad'|null) to the outcome label spans in the ClusterCard and HabitSide example rows (src/components/SessionPatterns.tsx:340-358, :566-571). The useSessionTags hook is already imported and provides setTag at line 660 — it is simply not destructured. This is a one-line destructure plus an onClick toggle, and it closes the biggest gap in the view: the user sees '[good]' / '[bad]' labels but cannot change them from here, defeating the habit-impact ranking loop.
- Add 'What to do about it' links on HURTS habit-factor cards (src/components/SessionPatterns.tsx:619-624). For each HURTS verdict, render a small Button variant=link that calls onNavigate (passed down or obtained via context) to the Recommendations view filtered to the matching detector key (compaction → 'compaction-hot-sessions', tool-errors → 'tool-undo-rate' / 'low-tool-effectiveness', etc.). The detector key is already on HabitFactor.key (line 382 of parse-timeline-success.ts). This converts the view's strongest signal into a concrete remediation path.
- Make the four ClusterCard metric mini-cards (Avg cost, Tool error %, Compacted %, Tool reuse %) clickable links to the relevant views (CostAttribution, ErrorRetry, ContextHealth, ToolUsage respectively), and apply the same pattern to the corresponding ComparePanel rows. Every cell in the compare panel that shows a 'worse' value is a dead-end today; making them links turns the compare workflow into a direct investigation pipeline.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Sessions analyzed (KPI tile) | orientation | none | NONE — dead-end | Cut or fold into a subtitle; the cluster cards already show the total implicitly. If kept, link to the sessions view. |
| Distinct shapes (KPI tile) | orientation | none | NONE — dead-end | Cut — its value is context for the ranked/singleton tiles below; it adds no action on its own. |
| Ranked (≥ 2 sessions) (KPI tile) | orientation | none | NONE — dead-end | Cut or merge into the 'Distinct shapes' tile as a denominator fraction, e.g. '8 of 14 shapes ranked'. |
| Singletons (KPI tile) | workflow-hygiene | low | NONE — dead-end | Add an expandable or drill-in that lists singleton session IDs (each as an onOpenSession link) so the user can review unusual one-off sessions and decide whether to tag them good/bad. **Superseded (2026-07-08):** #2349 / PR #2360 intentionally removed the Singletons and Distinct shapes tiles and folded both counts into the Ranked shapes sub-line; do not re-add this tile to implement the dated recommendation. |
| Habit-impact: labelled session count (header subtitle) | workflow-hygiene | low | NONE — dead-end | Add inline link 'label more sessions' that navigates to the sessions view, making the call-to-label explicit since more labels improve accuracy. |
| Habit-impact: proxy session count (header subtitle) | workflow-hygiene | low | NONE — dead-end | Same fix as labelled count — route user to sessions view to label more; or cut this number and keep only 'N sessions labelled by you, rest inferred'. |
| HabitFactor: verdict badge (HELPS / HURTS / NO CLEAR EFFECT) | workflow-hygiene | medium | NONE — dead-end | Wire to a recommendation: on 'HURTS' render a Button or inline link that deep-links into the Recommendations view filtered to the relevant detector (e.g. 'compaction-hot-sessions' for the compaction factor). |
| HabitFactor: factor title (e.g. 'Compacting context', 'Tool errors') | workflow-hygiene | medium | NONE — dead-end | Same as verdict badge fix — clicking the title on a HURTS card could jump to the corresponding recommendation or docs anchor. |
| HabitSide: side label (e.g. 'compacted' / 'not compacted') | orientation | none | NONE — dead-end | Keep — it is purely descriptive framing for the numbers beside it; no action needed at the label itself. |
| HabitSide: session count | orientation | none | NONE — dead-end | Keep as context; not a primary action target. |
| HabitSide: good-outcome rate % | workflow-hygiene | medium | NONE — dead-end | This is the core signal of the Habit Impact section. Add an onOpenSession-routed link that lists the sessions on this side, or a button 'See sessions' that navigates to sessions view pre-filtered to this factor-side. |
| HabitSide: avg cost | cost | low | NONE — dead-end | Keep as supporting context for the good-outcome rate; no standalone action needed. |
| HabitSide: tool error rate | success-rate | low | NONE — dead-end | Keep as supporting context; linking to ErrorRetry view for the specific sessions would be a medium-effort improvement. |
| HabitSide: example session ID links | discovery | high | onClick → openSession(sessionId) → navigate to sessions view focused on that session | keep — already actionable |
| HabitFactor: magnitude / verdict prose (e.g. '2.3× costlier on the compacted side. Association, not causation.') | workflow-hygiene | low | NONE — dead-end | Keep the quantification; add a single 'What to do about it' inline recommendation link for HURTS verdicts only. |
| ClusterCard: rank number (#1, #2 …) | orientation | none | NONE — dead-end | Keep — ordinal position is pure display context; no action needed. |
| ClusterCard: cluster name (e.g. 'Quick light-touch clean') | orientation | none | NONE — dead-end | Keep as label — the name itself requires no action. |
| ClusterCard: session count | orientation | low | NONE — dead-end | Make the count a link that navigates to the sessions view listing all sessions in this cluster; requires passing the cluster's session IDs or a filter key. |
| ClusterCard: goodOutcomeScore badge (Score 0–100) | workflow-hygiene | low | NONE — dead-end | The tooltip already explains the score formula. Adding a 'How to improve this score' link to the relevant Recommendations view would close the last mile. |
| ClusterCard: Pin button | workflow-hygiene | high | onClick → togglePin → adds cluster key to pinnedKeys state → ComparePanel appears when two are pinned | keep — already actionable |
| ClusterCard metric mini-card: Avg cost | cost | medium | NONE — dead-end | Add onClick to navigate to cost attribution view (TokenUsage or CostAttribution) pre-filtered to sessions in this cluster. |
| ClusterCard metric mini-card: Tool error % | success-rate | medium | NONE — dead-end | Add onClick to navigate to ErrorRetry view filtered to sessions in this cluster, or at minimum make it an onOpenSession link to the worst-error session in examples. |
| ClusterCard metric mini-card: Compacted % | context-health | medium | NONE — dead-end | Add onClick to navigate to ContextHealth view. Even a tooltip that says 'Reduce by splitting long sessions or compacting earlier' with a link would convert this to actionable. |
| ClusterCard metric mini-card: Tool reuse % | workflow-hygiene | medium | NONE — dead-end | Add onClick to navigate to ToolUsage view or Recommendations view; high tool-reuse typically means repeated bash retries — a recommendation exists for that detector. |
| SignatureBadges: rhythm label (rapid / sustained / sparse) | orientation | none | NONE — dead-end | Keep as display labels; the shape-level action belongs at the cluster card, not on individual badges. |
| SignatureBadges: tool density label (light / medium / heavy tools) | orientation | none | NONE — dead-end | Keep as display label. |
| SignatureBadges: error spike label (none / sporadic / repeated errors) | success-rate | low | NONE — dead-end | For 'repeated errors' make the label a link to the ErrorRetry view; that gives the badge a concrete action target without adding a new button. |
| SignatureBadges: compaction label (compacted / no compaction) | context-health | low | NONE — dead-end | For 'compacted' make the label a link to the ContextHealth view. |
| SignatureBadges: duration bucket label (<15m / 15-60m / 60-180m / >180m) | orientation | none | NONE — dead-end | Keep — duration is descriptive framing only. |
| Shape detail: describeSignature prose (expandable) | orientation | none | NONE — dead-end | Keep — it is plain-language labelling of the already-shown badges; it is correctly framed as a secondary detail panel. |
| ClusterCard example: session ID link | discovery | high | onClick → openSession(sessionId) → navigate to sessions view focused on that session | keep — already actionable |
| ClusterCard example: cost per session (inline text) | cost | low | NONE — dead-end | Keep as context alongside the session ID link; cost is already drillable via the session link. |
| ClusterCard example: duration per session (minutes, inline text) | orientation | none | NONE — dead-end | Keep as context; no standalone action target. |
| ClusterCard example: user tag / facet outcome label ([good] / [bad] / [outcome text]) | workflow-hygiene | medium | FALSE AFFORDANCE — styled with title='Your label' / 'From /insights' implying it is meaningful, but the user cannot change the label from this view; setTag is imported from use-session-tags but not wired in this component | Wire setTag(sessionId, 'good'\|'bad'\|null) to the label span (toggle on click) so the user can correct or add labels directly in the examples list without switching to the sessions view. The hook already provides setTag; it just is not used here. |
| ComparePanel: Sessions row (A vs B) | orientation | none | NONE — dead-end | Keep — it provides denominator context for the rates in the rows below. |
| ComparePanel: Avg cost row (A vs B) | cost | medium | NONE — dead-end | Highlight the worse cell in red and add a link to the CostAttribution view for that cluster's sessions — one click to investigate the costlier pattern. |
| ComparePanel: Error rate row (A vs B) | success-rate | medium | NONE — dead-end | Highlight worse cell and link to ErrorRetry view. The comparison panel is explicitly a 'which is worse' tool; the action should follow immediately. |
| ComparePanel: Compaction row (A vs B) | context-health | medium | NONE — dead-end | Highlight worse cell and link to ContextHealth view. |
| ComparePanel: Tool reuse row (A vs B) | workflow-hygiene | medium | NONE — dead-end | Highlight worse cell and link to ToolUsage or Recommendations view. |
| ComparePanel: Outcome score row (A vs B) | workflow-hygiene | low | NONE — dead-end | Keep as a roll-up; the per-row links on the other rows provide the actionable detail. |
| ComparePanel: cluster name + SignatureBadges in column header | orientation | none | NONE — dead-end | Keep — column headers are orientation only; the Clear button alongside already handles the one user action (unpin). |
| ComparePanel: Clear button (unpin cluster from slot A or B) | workflow-hygiene | high | onClick → onClear(cluster.key) → togglePin removes key from pinnedKeys → ComparePanel collapses when fewer than 2 pinned | keep — already actionable |

## Permissions (Collaboration & Permission Tracking) — **MIXED**

_Surface permission-mode misuse, dangerous command patterns, and prompt friction so the user can harden their settings.json allowlist/denylist and investigate risky sessions._

Top fixes: - Dangerous Commands table — Command cell is double-truncated (parser at 200 chars, then CSS overflow:hidden at 40ch) with no tooltip, expand, or copy affordance; the user literally cannot read the full command. Add a title= tooltip showing the full text and a copy-to-clipboard icon on the cell.
- Pattern badge and Entrypoint badge in both tables are styled (danger pill, brand-color monospace) but have no click handler — they look interactive but dead-end. Wire pattern badge to scroll/highlight the matching Policy Builder row (deny that pattern) and entrypoint badge to filter the table to that entrypoint.
- KPI tile 'Distinct Modes Seen' is pure orientation noise — the Mode Breakdown table one scroll below already shows every mode with counts. Cut the tile, and for the remaining three tiles add anchor links (or a scrollIntoView call) so clicking a KPI number takes the user to the relevant detail table.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| KPI tile — Bypass-mode Sessions | safety | medium | NONE — dead-end | Make the count a link that scrolls to / filters the Permission Mode Breakdown table to show only bypassPermissions rows, or opens a session-list filtered to those sessions via onOpenSession. |
| KPI tile — Dangerous Commands | safety | medium | NONE — dead-end | Make the count scroll to / anchor the Dangerous Bash Commands table below, or open a modal with the full list. Count alone with no drill-in is a dead-end. |
| KPI tile — Permission Changes | safety | low | NONE — dead-end | Anchor to the Permission Changes table, or cut the tile — the table already exists below and this just duplicates a count with no incremental action. |
| KPI tile — Distinct Modes Seen | orientation | none | NONE — dead-end | Cut it. The mode breakdown table one scroll below enumerates every mode with counts — this KPI adds nothing except a bare count with no next action. |
| Permission Mode Breakdown — Mode column | safety | medium | NONE — dead-end | Make each mode name a filter that narrows the Permission Changes table to rows where toMode equals that value, letting the user trace when and why that mode was entered. |
| Permission Mode Breakdown — Entries count | orientation | low | NONE — dead-end | The number alone has no next action. Either add a tooltip explaining what an 'entry' is (each JSONL line with that permissionMode), or remove the column in favor of Sessions only. |
| Permission Mode Breakdown — Sessions count | safety | medium | NONE — dead-end | Make the session count open a session list filtered to that mode so the user can investigate which sessions ran in bypassPermissions. |
| Prompt-prone Tools table — Tool name | workflow-hygiene | high | NONE — dead-end | Wire a click to pre-select that tool's row in the Policy Builder below (scroll + highlight the matching candidate row), so the user can immediately act on the friction by adding an allow rule. |
| Prompt-prone Tools table — Promptable Calls count | workflow-hygiene | low | NONE — dead-end | Informational once the user understands the column label. Keep but add a tooltip clarifying the approximation caveat (already in the header blurb but not surfaced per-row). |
| Prompt-prone Tools table — Sessions count | workflow-hygiene | low | NONE — dead-end | Keep. No additional affordance needed; the Policy Builder below is the action. Low-priority. |
| Prompt-prone Tools table — Share % | workflow-hygiene | low | NONE — dead-end | Keep as context for prioritizing Policy Builder rows. No standalone action needed. |
| Policy Builder — Rule column (label + 'now: X' badge) | safety | high | NONE — dead-end (informational label only) | keep — already actionable via the radio group on the same row; the label is load-bearing context for the radio decision. |
| Policy Builder — Why column (detail text) | safety | medium | NONE — dead-end | keep — already actionable context. Low priority to add a drill-in, but linking the session count in detail text to onOpenSession for the relevant sessions would raise actionability to high. |
| Policy Builder — Deny/Ask/Allow/No-change radio inputs per row | safety | high | onChange={() => setChoices((prev) => ({ ...prev, [c.rule]: action }))} — wired, updates diff preview | keep — already actionable |
| Policy Builder — diff count summary ('N change(s) to merge') | safety | low | NONE — dead-end | keep — informational enabler for the Copy/Write buttons below; no fix needed. |
| Policy Builder — diff snippet preview (pre block) | safety | medium | NONE — dead-end (no copy-on-click, no inline CopyButton on the pre block itself) | Add a CopyButton icon on the pre block itself so the user can copy without hunting for the 'Copy diff' button; it is a false-ease gap — the button exists but is visually separated from the snippet. |
| Policy Builder — 'Copy diff' button | safety | high | onClick={handleCopy} — copies snippet to clipboard, flashes 'Copied!' | keep — already actionable |
| Policy Builder — 'Write to settings.json' button (server mode only) | safety | high | onClick={openWritePreview} — opens confirmation dialog to write rules to ~/.claude/settings.json | keep — already actionable. Note: hidden in SPA build (SERVER_AVAILABLE=false); SPA users only get Copy diff with no guidance that the file path they need to edit is ~/.claude/settings.json. |
| Policy Builder — write confirmation dialog (deny/ask/allow rule lists with + prefix) | safety | high | onClick={writeToSettings} — POSTs diff to /api/policy/write, shows success/error status | keep — already actionable |
| Policy Builder — write status message (success/error banner) | safety | medium | NONE — dead-end | On success, add a link to open ~/.claude/settings.json path in a copy tooltip, or show the backup file path as a copy button so the user can verify. Currently the backup path is only mentioned in text. |
| Dangerous Commands table — Time column | orientation | none | NONE — dead-end | keep — timestamp is context for the row; no standalone action needed. |
| Dangerous Commands table — Session link (PfSessionIdLink) | discovery | high | onClick={(e) => { e.stopPropagation(); onOpen(sessionId); }} — opens the session detail overlay when onOpenSession is provided | keep — already actionable. Caveat: when onOpenSession is undefined (Permissions.tsx:49-52), the link renders as plain monospace text with no affordance and no fallback; the component silently degrades. Add a copy-to-clipboard fallback when onOpen is absent. |
| Dangerous Commands table — Entrypoint badge (EntrypointBadge) | safety | low | NONE — dead-end | make it a filter toggle: clicking sdk-* limits the table to unattended sessions (mirrors the global entrypointFilter). Currently the badge is styled (brand color for unattended) but has no click handler — false affordance risk. |
| Dangerous Commands table — Pattern badge (danger pill) | safety | medium | NONE — dead-end | Make the badge a filter: clicking 'rm -rf' narrows the table to that pattern. Alternatively link directly to the matching Policy Builder row to add a deny rule for that pattern. |
| Dangerous Commands table — Command column (truncated, no copy) | safety | medium | NONE — dead-end (truncated to 40ch via CSS overflow:hidden, no tooltip, no copy) | Add a click-to-expand or copy-to-clipboard on the cell. The command is already truncated to 200 chars by the parser and then further clipped by CSS — the user cannot read the full command without a tooltip or expand affordance. |
| Dangerous Commands table — 'Showing N of M total' note | orientation | low | NONE — dead-end | Add a 'Show all' or 'Load more' button matching the Permission Changes pattern. Currently there is no way to see commands 51+ without changing the code. |
| Permission Changes table — Timestamp column | orientation | none | NONE — dead-end | keep — timestamp is row context; no standalone action needed. |
| Permission Changes table — Session link (PfSessionIdLink) | discovery | high | onClick={(e) => { e.stopPropagation(); onOpen(sessionId); }} — opens session detail when onOpenSession provided | keep — already actionable. Same caveat: degrades to plain text when onOpenSession is absent. |
| Permission Changes table — Entrypoint badge | safety | low | NONE — dead-end | Same as Dangerous Commands entrypoint badge — styled but unclickable. |
| Permission Changes table — From column | safety | low | NONE — dead-end | keep — contextual; actionability comes from the To column + session link. |
| Permission Changes table — To column (danger-colored for bypassPermissions) | safety | medium | NONE — dead-end | The danger highlight signals risk but dead-ends. Add a click on bypassPermissions rows that opens the session via onOpenSession, giving the user a path from 'this session entered bypass mode' to seeing exactly what it did. |
| Permission Changes table — 'Load more' button | discovery | high | onClick={() => setVisibleChanges((v) => v + 50)} — loads next 50 rows | keep — already actionable |

## errors — **MIXED**

_Show the user which tools and API calls are failing and retrying so they can fix flaky commands, reduce backoff-induced latency, and improve overall Claude session reliability._

Top fixes: - Wire a PF Tooltip (or expand-on-click) to the Summary column and the Command/Label column in the Error Retry Sequences table — both truncate with CSS ellipsis but have no hover/expand/copy affordance, so the most diagnostic text in the view is routinely hidden with no escape hatch.
- Add per-status-code actionable guidance (tooltip or inline label) to the API Error Status Codes table: 429 = rate-limit (reduce concurrency), 529 = Anthropic overloaded (back off), ConnectionRefused = network/proxy issue. The view correctly identifies the error codes but leaves the user to Google each one.
- Make the six KPI tiles scroll-anchor to their corresponding detail section (or link to a filtered view): all six are dead-ends today. Even a simple `href='#errors-top-tools-by-error-rate'` on 'Total Tool Errors' would convert decoration into navigation.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Total Tool Errors (KPI tile) | success-rate | low | NONE — dead-end | Make tile clickable: scroll-anchor to or filter the 'Top Tools by Error Rate' chart below, or link to the ToolUsage view pre-filtered to errored calls |
| Overall Error Rate (KPI tile) | success-rate | low | NONE — dead-end | Add threshold context (e.g. '>5% is high') and link to the chart section; a plain percentage with no benchmark gives the user nothing to act on |
| Retry Groups Detected (KPI tile) | success-rate | low | NONE — dead-end | Scroll-anchor to the 'Top Retry Groups' table below; the count is only useful if the user can inspect which sessions/tools drive it |
| API Errors (KPI tile) | success-rate | low | NONE — dead-end | Scroll-anchor to the 'API Error Events' table at the bottom; without navigation the count is decorative |
| Native (system/api_error) (KPI tile) | success-rate | low | NONE — dead-end | Add a tooltip explaining what 'native' means (structured vs text-matched), then scroll-anchor to the status-codes table; red accent with no drill-down is alarming but not actionable |
| Retry Pressure (attempts / backoff) (KPI tile) | speed | medium | NONE — dead-end | This is the most actionable KPI (backoff seconds directly measures time lost to API overload), but has no drill-down. Add scroll-anchor to the status-codes table and expose maxAttempt/maxRetries; consider a copy button so the user can paste the stat into a support ticket |
| Top Tools by Error Rate — bar chart (tool name + error rate % + error/total label) | success-rate | medium | NONE — dead-end | Wire a bar onClick to navigate to the ToolUsage view filtered to that tool, or at minimum scroll-anchor to the error-retry sequences table filtered by tool name; a chart bar that shows an 80% error rate with no way to investigate is a missed action |
| Retry Groups table — Session column (session ID link) | discovery | high | onClick → onOpenSession(sessionId) → navigates to Sessions view focused on that session | keep — already actionable |
| Retry Groups table — Tool column | success-rate | medium | NONE — dead-end | Make tool name clickable to cross-filter the Error Retry Sequences table or link to ToolUsage view; same tool appearing in multiple retry groups is a signal worth drilling into |
| Retry Groups table — Count column | success-rate | low | NONE — dead-end | keep count as-is; the session link on the same row already provides the drill-in path — low priority |
| Retry Groups table — Time Window (start → end timestamps) | speed | medium | NONE — dead-end | The time window is only useful if the user can correlate it to the session timeline. Wire the session ID link to open the session at that timestamp range, or add a copy button for the timestamp pair |
| Retry Groups table — Errors? badge (red 'yes' / dash) | success-rate | low | NONE — dead-end | Low-value binary flag; if hasErrors=false the retry group is noise (just slow calls). Consider hiding groups with no errors by default or adding a filter toggle to focus on error-containing groups |
| Error Retry Sequences table — Tool column | success-rate | medium | NONE — dead-end | Link to ToolUsage view filtered to that tool; the user can then see the full call history and error breakdown |
| Error Retry Sequences table — Command/Label column (truncated with ellipsis) | success-rate | medium | FALSE AFFORDANCE — ellipsis implies more content exists but there is no expand/tooltip/copy handler | Add a PF Tooltip on hover to show the full command text, and/or a CopyButton; a truncated Bash command that the user cannot read or copy is worse than not showing it |
| Error Retry Sequences table — Retries count | success-rate | low | NONE — dead-end | No session link on these rows. Add session context (which sessions exhibit this pattern) so the user can investigate; a retry count without a session drill-in is orientation-only |
| API Error Status Codes table — Code column (HTTP status or cause code) | success-rate | high | NONE — dead-end | Add a link/tooltip mapping known codes to actionable guidance: 429 → reduce concurrency or add delays; 529 → Anthropic overloaded, retry later; ConnectionRefused → check network/proxy. Each code has a concrete remediation the user should take |
| API Error Status Codes table — Kind column (HTTP status vs cause code) | orientation | none | NONE — dead-end | Cut or collapse into a badge on the Code column; the distinction between 'HTTP status' and 'cause code' is developer-facing metadata, not user-actionable |
| API Error Status Codes table — Count column | success-rate | low | NONE — dead-end | Acceptable as supporting context for the Code column; no fix needed beyond adding the code-level guidance (see Code column fix) |
| API Error Status Codes table — Sessions column | success-rate | medium | NONE — dead-end | Make the session count a link that navigates to Status Codes by Session table filtered to that code, or add a tooltip listing the affected session IDs (currently visible only in the separate per-session table) |
| API Error Status Codes table — Repeats/Session column | success-rate | medium | NONE — dead-end | High repeats/session for a 429 or 529 directly signals a specific session to investigate; add a link to filter the per-session table by this code or highlight the worst offender session |
| Status Codes by Session table — Session column (session ID link) | discovery | high | onClick → onOpenSession(sessionId) → navigates to Sessions view focused on that session | keep — already actionable |
| Status Codes by Session table — Total column | success-rate | low | NONE — dead-end | Supported by the session link on the same row; no additional affordance needed |
| Status Codes by Session table — Codes column (e.g. '529×3, 429×1') | success-rate | medium | NONE — dead-end | Each code badge should carry the same actionable tooltip as in the Status Codes table (429 → rate limit, 529 → overloaded). The session link gets the user to the session; the codes guide what to look for once there |
| API Error Events table — Timestamp column | orientation | none | NONE — dead-end | Orientation-only; timestamp provides temporal context but there is no time-range filter or calendar link to act on it. Accept as-is — the session link on the same row is the action path |
| API Error Events table — Session column (session ID link) | discovery | high | onClick → onOpenSession(sessionId) → navigates to Sessions view focused on that session | keep — already actionable |
| API Error Events table — Summary column (truncated with ellipsis, maxWidth 36rem) | success-rate | high | FALSE AFFORDANCE — ellipsis implies more content but no expand/tooltip/copy handler is wired | Add a PF Tooltip on hover to show the full summary string (capped at 200 chars in the parser). The summary is the primary diagnostic signal for what went wrong — truncating it without an expand path defeats the purpose of the column |

## Context Health — **MIXED**

_Surface per-session context-window pressure diagnostics so the user can identify which sessions are burning cash on re-sent context, hitting compaction, or are at risk of doing so, and then act on those sessions to improve context discipline._

Top fixes: - Make every Reason badge in the Health Scores table a navigational link: 'Low cache hit rate' scrolls to the Cache-read ratio table, 'N compactions' scrolls to the Top Compactions table, 'Over window'/'Peak context' fires onOpenSession — these reason badges are the densest actionable signal on the view yet are currently pure dead-ends.
- Wrap the Top Suggestion cell in the Compaction Risk table in a PF Popover that shows all suggestions and provides a one-click 'Copy' per suggestion line. The suggestions already contain ready-to-paste slash-commands (/compact, /clear) and file-path advice; making them copyable is a near-zero-cost implementation that directly closes the loop from diagnosis to action.
- Wire all false-affordance accented/colored cells (Reduction % in compactions table, Score in health table, Risk badge in compaction-risk table) to onOpenSession — each already has the session ID in scope, the styling already signals 'this is important', and the missing onClick is a one-liner per cell.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Sessions Analyzed (KPI tile) | orientation | none | NONE — dead-end | Cut it or replace with a '% of sessions with any health flag' number that links directly to the health-score table below. |
| Avg Cache Hit Rate (KPI tile) | context-health | low | NONE — dead-end | Add onClick that scrolls to / highlights the Cache-read ratio table so the user lands on the per-session rows they need to fix. |
| Total Compactions (KPI tile) | context-health | low | NONE — dead-end | Add onClick that scrolls to the Top Compactions table, or show the count as a badge next to the table header. |
| Sessions with Score < 50 (KPI tile, accented when > 0) | context-health | medium | NONE — dead-end | Make the tile a button/link that scrolls to the Lowest Session Health Scores table and pre-filters to rows with score < 50. |
| Total Est. Cost (KPI tile) | cost | none | NONE — dead-end | Cut it — total spend is already a primary KPI in the Cost view; showing it here without per-session drill-in is pure duplication. |
| Context-read cost + % of total (KPI tile, accented when >= 50%) | cost | medium | NONE — dead-end | Add onClick to scroll to the health-score table where per-session cache-read cost is shown, so the user can identify which session is driving the figure. |
| Context tax / msg (KPI tile — avg cache-read tokens per API call) | context-health | medium | NONE — dead-end | Add onClick to scroll to the health-score table (which has a per-session Ctx-read/msg column), or surface the top offending session inline as sub-text. |
| Sessions Over 200K Window (KPI tile, accented when > 0) | context-health | high | NONE — dead-end | Make it a button that scrolls to / filters the Compaction Risk table showing only sessions with peakContextPct >= 100, or directly opens the worst session via onOpenSession. |
| Worst Cache Hit Rates chart — bar per session (top 15, horizontal bar) | discovery | high | onClick → onOpenSession — wired only when onOpenSession is supplied; silent dead-end when prop is absent | keep — already actionable (when onOpenSession is wired). Add a visual affordance (cursor pointer, tooltip 'Click to open session') so the interaction is discoverable. |
| Cache-read ratio table — session ID cell (per row) | discovery | high | onClick → onOpenSession — PF Button link variant when prop is present; plain mono span when absent | keep — already actionable when onOpenSession is wired. |
| Cache-read ratio table — Reads column (cache-read tokens per session) | context-health | low | NONE — dead-end | keep as-is — it is a supporting column that provides context for the hit-rate figure; consider removing it to reduce column noise and relying on the hit-rate alone. |
| Cache-read ratio table — Writes column (cache-creation tokens per session) | context-health | low | NONE — dead-end | Consider dropping this column (same argument as Reads — it is denominator noise once the hit-rate % is shown). |
| Cache-read ratio table — Hit-rate % (per row, flagged bold+accented when < 50%) | context-health | medium | FALSE AFFORDANCE — flagged rows render bold/brand-color styling that implies urgency but clicking the cell does nothing; only the session ID cell is clickable | Wire the low-hit-rate row or cell to onOpenSession, or add a copy/clipboard button so the user can grab the session ID to investigate. |
| Cache-read ratio table — count of sessions under 50% / total sessions with cache activity (header badge) | orientation | low | NONE — dead-end | The count is a useful summary; it becomes actionable if clicking it scrolls to the first flagged row. |
| Cache-read ratio table — sort buttons (Reads / Writes / Hit-rate %) | discovery | medium | onClick → onSortChange(key) — PF plain Button in each column header | keep — already actionable. |
| Top Compactions table — session ID cell (per row) | discovery | high | onClick → onOpenSession — PF Button link when prop is present | keep — already actionable when onOpenSession is wired. |
| Top Compactions table — Timestamp column (per row) | orientation | none | NONE — dead-end | keep as contextual data — it is supporting info for the session, not a primary action driver. |
| Top Compactions table — Before tokens column (per row) | context-health | low | NONE — dead-end | keep as supporting column for the Reduction % figure. |
| Top Compactions table — After tokens column (per row) | context-health | low | NONE — dead-end | keep as supporting column. |
| Top Compactions table — Reduction % column (per row, bold + brand color) | context-health | medium | FALSE AFFORDANCE — styled with brand color and fontWeight 600 (implying importance/clickability) but no click handler exists | Wire the entire compaction row to onOpenSession (using the session ID already in scope), so the user can drill into the session where the compaction occurred. |
| Lowest Session Health Scores table — session ID cell (per row) | discovery | high | onClick → onOpenSession — PF Button link when prop is present | keep — already actionable when onOpenSession is wired. |
| Lowest Session Health Scores table — Score column (per row, accented when < 50) | context-health | high | FALSE AFFORDANCE — accented brand-color styling when score < LOW_HEALTH_SCORE, but the cell itself has no click handler (only the adjacent SessionIdCell does) | Wire the Score cell (or the whole row) to onOpenSession, matching the affordance the color implies. The session ID is available as row.sessionId. |
| Lowest Session Health Scores table — Compactions count column (per row) | context-health | low | NONE — dead-end | keep as supporting diagnostic column. |
| Lowest Session Health Scores table — Cost column (per row, monospace) | cost | low | NONE — dead-end | keep as supporting column. |
| Lowest Session Health Scores table — Cache-Read $ column (per row, with tooltip showing % of session cost) | cost | medium | NONE — dead-end | keep — the tooltip (% of session cost) is contextually useful. Consider linking this cell to the Cost view filtered to this session if that cross-view navigation exists. |
| Lowest Session Health Scores table — Ctx-read / msg column (per row) | context-health | medium | NONE — dead-end | keep as diagnostic support column. |
| Lowest Session Health Scores table — Reasons badges (per row, PF Label outline/grey) | context-health | high | NONE — dead-end | Each reason badge should be a link or button: 'Low cache hit rate' should scroll to Cache-read ratio table; 'N compactions' should scroll to Top Compactions table; 'Over window' and 'Peak context' should open the session. These are the highest-value actionable signals on the view. |
| Compaction Risk — % of sessions running hot + (hot of total) header stat | orientation | low | NONE — dead-end | keep as summary framing; actionability is provided by the table rows below. |
| Compaction Risk — risk-class distribution bar (Low / Medium / High segments) | context-health | low | NONE — dead-end (segments have a title tooltip but no click handler) | Wire each segment to filter the risk table below to that risk class, giving the bar the drill-in action the visual encoding implies. |
| Compaction Risk — risk-class distribution legend (Low / Medium / High counts) | orientation | none | NONE — dead-end | Merge into the bar tooltips / cut legend text — it duplicates the bar and adds no action path. |
| Compaction Risk table — session ID cell (per row) | discovery | high | onClick → onOpenSession — PF Button link when prop is present | keep — already actionable when onOpenSession is wired. |
| Compaction Risk table — Risk score label badge (per row, PF Label colored by risk class) | context-health | medium | NONE — dead-end | Wire the risk badge or the entire row to onOpenSession (session ID is row.sessionId in scope). The badge is visually prominent but produces no action. |
| Compaction Risk table — Peak % column (per row, monospace) | context-health | low | NONE — dead-end | keep as supporting diagnostic column. |
| Compaction Risk table — Growth/turn column (per row, monospace tokens) | context-health | low | NONE — dead-end | keep as supporting diagnostic column. |
| Compaction Risk table — Large outputs column (X of Y, Z%) (per row) | workflow-hygiene | medium | NONE — dead-end | keep the column; the top suggestion row already tells the user to 'use offset/limit/Grep' — these two could be linked so clicking the count opens the session in the tool-use view. |
| Compaction Risk table — Re-reads column (X of Y, Z%) (per row) | workflow-hygiene | medium | NONE — dead-end | keep the column; consider making it a link into the session view to see which files were re-read, or at minimum show the top re-read filename in a tooltip (the data is available in parse-compaction-risk: topReadPath). |
| Compaction Risk table — Compactions observed count column (per row) | context-health | low | NONE — dead-end | keep as supporting column. |
| Compaction Risk table — Growth sparkline (per row, ChartLine colored by risk class) | context-health | medium | NONE — dead-end | Wire a click on the sparkline div to onOpenSession so the user can drill into the session whose shape they are inspecting. |
| Compaction Risk table — Top suggestion text (per row, title tooltip shows all suggestions) | context-health | high | NONE — dead-end (hover reveals full suggestions list via title tooltip, but no click action) | This is the highest-value data point in the entire view. Replace the plain span with a copyable chip or PF Popover that shows all suggestions and provides a 'Copy suggestion' button, plus a link to open the session. Suggestions include slash-commands (/compact, /clear) that the user should be able to copy in one click. |
| Compaction Risk table — sort buttons (Risk / Peak % / Growth / Large outputs / Re-reads / Compactions column headers) | discovery | medium | onClick → onSortChange(key) — PF plain Button in each header | keep — already actionable. |

## tokens — **MIXED**

_Surface token spend, cache efficiency, and model mix so the user can identify which sessions and models are driving cost and take action to reduce it._

Top fixes: - Wire the Compaction Events table Session column to onOpenSession (matching the Top-20 Sessions table pattern at line 1022-1030) — a session with a 70%+ compaction reduction is the highest-signal prompt-hygiene finding on the view and currently goes nowhere.
- Add a CTA to the Opus-swap ceiling rows (Opus → Sonnet and Opus → Haiku) that links to the settings view or CLAUDE.md where the default model is configured — the saving is computed and displayed but the path to acting on it is entirely absent, making these the most expensive dead-ends in the view.
- Elevate Cache Hit Rate from a plain KPI tile to a threshold-badged signal (green/red at a configurable threshold, e.g. 50%) and make it the primary cache metric, replacing or demoting the raw Cache Writes / Cache Reads tiles — it is the only cache number the user can act on directly and is currently buried in the second KPI row.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Input Tokens (KPI tile) | context-health | low | NONE — dead-end | Make tile clickable: scroll-to or filter the Top-20 Sessions table by input descending, so the user can immediately see which sessions drove the input volume. |
| Output Tokens (KPI tile) | orientation | none | NONE — dead-end | Cut or merge with Input Tokens tile; output tokens alone carry no decision without ratio context. Alternatively show output/input ratio as the primary number. |
| Cache Writes (KPI tile) | context-health | medium | NONE — dead-end | Merge into a single 'Cache Efficiency' tile showing hit-rate prominently; add a tooltip linking to the Compaction Events table below. |
| Cache Reads (KPI tile) | context-health | medium | NONE — dead-end | Same as Cache Writes — merge into a Cache Hit Rate tile that is the primary number, not a derived secondary metric buried in the second row. |
| Est. Cost (KPI tile) | cost | medium | NONE — dead-end | Make clickable: scroll to Opus-swap ceiling table, which is the direct 'what can I do about this cost' answer. |
| Sessions Loaded (KPI tile) | orientation | none | NONE — dead-end | Cut it. The count of loaded sessions is a loader-state indicator, not a cost or efficiency signal. It belongs on an upload/data-source widget, not a KPI row. |
| API Calls (KPI tile) | orientation | low | NONE — dead-end | Replace or augment with 'Avg tokens/call' so the user can spot runaway context growth. Raw call count alone is vanity. |
| Cache Hit Rate (KPI tile) | context-health | high | NONE — dead-end | This is the most actionable KPI on the view — a low rate means the user should extend CLAUDE.md caching or restructure prompts. Add an inline threshold badge (e.g. green/red at 50%) and link to a recommendation or the compaction events table. |
| 7-day burn rate ($/day) in Spend Over Time card header | cost | high | NONE — dead-end | Add a budget comparison: accept a user-configured monthly budget (stored in settings or passed as prop) and color the burn rate red/yellow/green. Without a target, the number has no decision threshold. |
| Date range display (firstDay → lastDay) in Spend Over Time header | orientation | none | NONE — dead-end | Cut the monospace date range from the card header; it is context for the chart axis, which already shows the dates. Dead weight. |
| Projected month-end cost vs last month (narrative sentence) | cost | high | NONE — dead-end | Wire the projected cost number to scroll to the Opus-swap ceiling table — the user can then see the theoretical maximum saving available to close the gap. |
| Last month actual cost (in projection sentence) | cost | low | NONE — dead-end | Keep as context for the projection but the delta percentage arrow already summarizes it; no additional affordance needed beyond the existing fix for projected cost. |
| Month-over-month delta percentage (▲/▼ badge in projection sentence) | cost | medium | NONE — dead-end | Same fix as projected month-end cost: scroll to Opus-swap ceiling table on click. |
| Undated spend warning (excluded from daily series) | workflow-hygiene | low | NONE — dead-end | Link to a filtered list of sessions whose entries have no parseable timestamp so the user can identify and fix the source. |
| Spend Over Time area chart (daily $) | cost | medium | NONE — dead-end | Wire click on a data point to filter the Top-20 Sessions table to that calendar day, or navigate to the Sessions view pre-filtered to that date. |
| Tokens by Session stacked bar chart (Top 20 sessions) | discovery | high | onClick → onOpenSession(sessionId) at TokenUsage.tsx:598-601; wired in App.tsx:526 → openSession → setFocusSessionId + navigateTo('sessions') at App.tsx:253-258 | keep — already actionable |
| Tokens by Session legend (Input / Output / Cache Write / Cache Read swatches) | orientation | none | NONE — dead-end | Legend is context for the chart; acceptable as-is. Optionally make legend items toggle-filter the chart series. |
| Model Distribution donut chart (calls per model, top 5) | cost | medium | NONE — dead-end | Wire click on a donut segment to filter the Model Mix table below to that model row, or scroll to it. The donut already maps to the Model Mix data — the two widgets should be linked. |
| Model Distribution legend (model name + call count per model) | cost | medium | NONE — dead-end | Make each legend row clickable to filter the Top-20 Sessions table to sessions that used that model, enabling the user to find specific sessions to investigate. |
| Compaction Events table — Session column (truncated session ID) | context-health | high | NONE — dead-end | Render the session ID as a Button variant=link calling onOpenSession, matching the pattern in the Top-20 Sessions table (line 1022-1030). This is the highest-value fix in the whole view: a compaction event with 80%+ reduction is the clearest signal the user has that they should review that session's prompt structure. |
| Compaction Events table — Timestamp column | orientation | none | NONE — dead-end | Keep as context; no independent affordance needed beyond the session drill-in fix above. |
| Compaction Events table — Before context tokens | context-health | low | NONE — dead-end | Keep as context for the reduction percentage. Tooltip explaining what 'before context' means (input + cache) would help but is cosmetic. |
| Compaction Events table — After context tokens | context-health | low | NONE — dead-end | Same as Before — context for reduction; keep. |
| Compaction Events table — Reduction % column | context-health | high | NONE — dead-end | Color-code the cell (e.g. red for >70%, yellow for 40-70%) to prioritize which rows demand action. The table is sorted by reduction descending, which is good, but there is no visual hierarchy to guide the eye. |
| Compaction Events — Load more button | context-health | medium | onClick → setCompactionVisible at TokenUsage.tsx:776-778 | keep — already actionable |
| Model Mix table — Model name column (with unknown-model warning badge) | cost | medium | NONE — dead-end | For rows where isUnknown=true, wire the warning badge to the recommendations view (legacy-model-overpay or unknown-model detector) so the user knows how to fix the pricing gap. |
| Model Mix table — Calls column | orientation | low | NONE — dead-end | No independent action; acceptable as context for cost-per-call math. Could show cost/call as a derived column instead. |
| Model Mix table — Tokens column | orientation | low | NONE — dead-end | Same as Calls — context; acceptable. |
| Model Mix table — Cost column ($) | cost | high | NONE — dead-end | Make the row clickable to filter the Top-20 Sessions table to sessions that used that model. The user who sees 'opus-4: $12.40' needs a way to find and review those sessions immediately. |
| Model Mix table — % of total cost column | cost | medium | NONE — dead-end | Same as Cost column fix — row drill-in. The % column is the most scannable cost-share signal but goes nowhere. |
| Opus-swap ceiling table — As-is (actual) row | orientation | none | NONE — dead-end | Keep as baseline reference row; no affordance needed. |
| Opus-swap ceiling table — Opus → Sonnet row (cost + saving $/%) | cost | high | NONE — dead-end | Add a 'Apply this model in settings' button or link to the CLAUDE.md / settings view where the user can change the default model. The saving number is compelling but the path to acting on it is completely absent. |
| Opus-swap ceiling table — Opus → Haiku row (cost + saving $/%) | cost | high | NONE — dead-end | Same as Opus → Sonnet: link to settings/CLAUDE.md where the default model is configured. A second CTA pointing to the 'legacy-model-overpay' recommendation (if triggered) would also close the loop. |
| Top 20 Sessions table — Session column (link button or plain span) | discovery | high | onClick → onOpenSession(row.sessionId) at TokenUsage.tsx:1026; App.tsx:253-258 navigates to 'sessions' view with focusSessionId | keep — already actionable |
| Top 20 Sessions table — Input column (sortable) | context-health | medium | onClick on column header at TokenUsage.tsx:985 → handleSessionSort; cell is NONE — dead-end | keep column sort — already actionable. Cell value alone is a dead-end; session-row click (Session column) covers the drill-in. |
| Top 20 Sessions table — Output column (sortable) | orientation | low | onClick on column header at TokenUsage.tsx:991 → handleSessionSort | Column sort keep. Output tokens alone are less actionable than cost; acceptable as secondary sort key. |
| Top 20 Sessions table — Cache Write column (sortable) | context-health | medium | onClick on column header at TokenUsage.tsx:997 → handleSessionSort | keep — sorting by cache write is actionable for finding high-write sessions. Cell is a dead-end but session drill-in covers it. |
| Top 20 Sessions table — Cache Read column (sortable) | context-health | medium | onClick on column header at TokenUsage.tsx:1003 → handleSessionSort | keep — same rationale as Cache Write column. |
| Top 20 Sessions table — Cost column (sortable) | cost | high | onClick on column header at TokenUsage.tsx:1009 → handleSessionSort | keep — already actionable as default sort; combined with session drill-in this is the primary cost-reduction workflow. |

## Tool Usage (tools view) — **MIXED**

_Surface which tools Claude is using (and misusing) so the user can fix their agent config, eliminate bypassed native tools, wrap repeated commands in skills/hooks, and tune tool effectiveness._

Top fixes: - Add CopyButton to every truncated command/pattern cell (Top Bash Commands, Repeated Commands, Discovery Failures example command, Native-Tool Bypass hint) — these are the most frequent high-actionability dead-ends, and a copy button is a one-line JSX change that directly enables the stated action ('wrap in a skill', 'add to CLAUDE.md').
- Fix the false affordance on the Native-Tool Bypass 'Native tool' column (ToolUsage.tsx:780-785): it is styled brand-blue like a clickable link but has no handler. Either add an href to Claude docs or remove the color — this is a trust-eroding UI bug.
- Wire the 'review' effectiveness badge (line 1064) as a button that navigates to the ErrorRetry view filtered to that tool (onOpenSession pattern already exists in the app), and add a click-through from the Overall Error Rate KPI tile to the Tool Effectiveness table sorted by '→ Error' descending — these two changes turn the two highest-visibility 'see a problem, do nothing' moments into genuine action entry points.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| KPI: Total Tool Calls | orientation | low | NONE — dead-end | Link to the Tool Breakdown table inline (anchor scroll) or add a 'See breakdown' button that focuses the table. Alternatively drop it — the bar chart immediately below conveys the same count with more granularity. |
| KPI: Unique Tools Used | orientation | low | NONE — dead-end | Cross-link to the Catalog Utilization section (how many of those are from your MCP config) so the number triggers a question: 'Am I loading more than I need?' Otherwise cut it. |
| KPI: Overall Error Rate | success-rate | medium | NONE — dead-end | Make it a button or anchor that jumps to the Tool Effectiveness table sorted by '→ Error' descending, so the number immediately leads to 'which tool is to blame'. |
| KPI: Sessions Analyzed | orientation | none | NONE — dead-end | Cut it. Knowing how many sessions were analyzed does not drive any config, file, or behavior change. |
| Catalog Utilization KPI: Sessions with manifest | orientation | none | NONE — dead-end | Cut it. It is a data-quality indicator that explains the denominator; it never tells the user to do anything. |
| Catalog Utilization KPI: Total catalog slots loaded | workflow-hygiene | low | NONE — dead-end | Combine with utilization pct into a single 'X of Y slots used (Z%)' tile that links to the 'Loaded but never invoked' table. |
| Catalog Utilization KPI: Used slots | orientation | none | NONE — dead-end | Fold into a 'X used / Y loaded' composite KPI; standalone 'used slots' count is a dead-end. |
| Catalog Utilization KPI: Catalog utilization % | workflow-hygiene | medium | NONE — dead-end | Add a threshold badge (e.g. red if < 50%) and anchor scroll to the 'Loaded but never invoked' table. The percent alone without a drill-path is orientation, not action. |
| Loaded-but-rarely-invoked table: Tool column | workflow-hygiene | high | NONE — dead-end | For MCP tool names (mcp__<server>__<tool>), add a copy button so the user can paste the server name into their mcp.json to remove it. Optionally cross-link to the MCP config file path shown elsewhere in the app. |
| Loaded-but-rarely-invoked table: Sessions loaded / Sessions used / Never used columns | workflow-hygiene | high | NONE — dead-end | Sort is already on 'never used'; that is fine. The fix is at the tool-name level: add a 'Remove from config' hint or a copy button for the server name. The numbers themselves are already clearly labeled and need no separate fix. |
| Per-session utilization table: Session ID | discovery | high | onClick → onOpenSession → navigateTo('sessions') — opens session timeline | keep — already actionable |
| Per-session utilization table: Loaded / Used columns | workflow-hygiene | low | NONE — dead-end | These are context for the utilization % on the same row; acceptable as supporting data. If the session link is clickable these are fine as-is. |
| Per-session utilization table: Utilization % | workflow-hygiene | medium | NONE — dead-end | Color-code the cell (red < 30%) to make poor sessions visually obvious, and ensure the session-id link on the same row is consistently rendered (currently only renders when onOpenSession is provided — which it is in App.tsx, but the fallback plain span is a false-affordance risk in test/SPA modes). |
| Tool Frequency bar chart (top 20 tools by call count) | workflow-hygiene | medium | ChartTooltip on hover shows 'tool: N calls' — read-only tooltip, no click through | Wire onClick on each bar to scroll to that tool's row in the Tool Breakdown table below, or highlight the row. A bar chart without drill-down is decorative once the table exists. |
| Tool Breakdown table: Tool name column | success-rate | medium | NONE — dead-end | For MCP tool names, add a copy button. For any tool with errorRate > threshold, add an inline badge linking to the Tool Effectiveness table row for that tool. |
| Tool Breakdown table: Calls count column (sortable) | workflow-hygiene | medium | Column header onClick toggles sort — functional interactive control | keep — already actionable (sort is the action). Could cross-link to per-session drill if value is high. |
| Tool Breakdown table: Errors count column (sortable) | success-rate | high | Column header onClick toggles sort — functional interactive control | keep — already actionable via sort. The cell value itself has no click-through; adding a link from any non-zero error count to the ErrorRetry view pre-filtered to that tool would make the datum more actionable. |
| Tool Breakdown table: Error Rate % column (sortable) | success-rate | high | Column header onClick toggles sort; cell value is plain text | Color-code cell using scoreColor() (already used in effectiveness table) — the visual signal turns a number into an action prompt. No other fix needed if coloring is added. |
| Top Bash Commands table: Command column | workflow-hygiene | high | NONE — dead-end (text is truncated with ellipsis, no copy button) | Add a CopyButton to each command cell. Commands are the exact text the user would put into a skill/hook/Makefile target; copy is the cheapest path to action. |
| Top Bash Commands table: Count column | workflow-hygiene | medium | NONE — dead-end | Supporting data for the command column. Acceptable as-is if the command gets a copy button. |
| Native-Tool Bypass ratio: Grep native : Bash | workflow-hygiene | high | NONE — dead-end | The ratio itself is actionable (add a CLAUDE.md instruction to prefer native Grep) but the UI offers no path to act. Add a 'Copy fix hint' button that copies the recommended CLAUDE.md snippet, or link to the Permissions/config view. |
| Native-Tool Bypass ratio: Glob native : Bash find | workflow-hygiene | high | NONE — dead-end | Same fix as Grep ratio: add a 'Copy fix hint' or link to CLAUDE.md editing path. |
| Native-Tool Bypass table: Bash pattern column | workflow-hygiene | medium | NONE — dead-end | Add a copy button on the pattern. Users who want to grep their CLAUDE.md for existing rules need the exact pattern string. |
| Native-Tool Bypass table: Native tool column | workflow-hygiene | medium | FALSE AFFORDANCE — styled brand-blue (color: var(--pf-t--global--color--brand--default)) like a link, but has no onClick or href | Either add an href linking to Claude docs for that native tool, or remove the blue color so it is not mistaken for a link. |
| Native-Tool Bypass table: Count column | workflow-hygiene | low | NONE — dead-end | Supporting data; acceptable as-is. No standalone fix needed. |
| Native-Tool Bypass table: Hint column | workflow-hygiene | high | NONE — dead-end | Add a 'Copy hint' button so the user can paste the hint text directly into CLAUDE.md. A plain text instruction with no copy path forces manual retyping. |
| Bash Commands by First Token bar chart (token + count + cumulative %) | workflow-hygiene | medium | ChartTooltip shows 'token: N calls (cumulative: X%)' — read-only, no drill-down | Wire onClick on each bar to filter the Top Bash Commands table to that token prefix. The chart's own text says 'high-frequency tokens are good candidates for a skill or Makefile target' — that suggestion needs a 'Create skill' or 'Copy commands' button to be actionable. |
| Repeated Commands table: Command column | workflow-hygiene | high | NONE — dead-end | Add a CopyButton. The body text already says 'wrap these in a hook, skill, or Makefile target' — the copy button is the one-click path to doing that. |
| Repeated Commands table: Total count / Sessions / Max per session columns | workflow-hygiene | low | NONE — dead-end | Supporting data; acceptable as-is once the command column gets a copy button. |
| Discovery Failures table: Skill column | workflow-hygiene | high | NONE — dead-end | The skill id is a directory name under ~/.claude/commands or the skills path. Add a copy button for the skill id, and/or a 'View skill' link that navigates to the AgentSkill view filtered to that skill so the user can inspect its description and update the trigger keywords. |
| Discovery Failures table: Matched keywords column | workflow-hygiene | medium | NONE — dead-end | Supporting data — the keywords explain the match. Acceptable as-is; actionability is driven by the skill column. |
| Discovery Failures table: Example command column | workflow-hygiene | high | NONE — dead-end | Add a CopyButton. The example command is the exact text the skill description should reference; users need it to update the skill's trigger wording. |
| Discovery Failures table: Sessions count (sortable) | workflow-hygiene | medium | Column header onClick toggles sort — functional | keep — already actionable via sort. |
| Tool Effectiveness table: Tool name column | success-rate | medium | NONE — dead-end | Cross-link to the Tool Breakdown table row for the same tool (anchor scroll or highlight) so the user can see absolute call volume alongside the effectiveness score. |
| Tool Effectiveness table: Score column (sortable, color-coded) | success-rate | high | Column header onClick toggles sort; cell value is color-coded by score threshold — functional affordances | keep — already actionable. Sort + color together make the score actionable. |
| Tool Effectiveness table: Calls (invocations) column (sortable) | success-rate | medium | Column header onClick toggles sort — functional | keep — already actionable via sort. |
| Tool Effectiveness table: → Error column (sortable) | success-rate | high | Column header onClick toggles sort — functional; tooltip explains definition | keep — already actionable. Could gain a link from the error count to the ErrorRetry view filtered to that tool, but the sort already makes this datum useful. |
| Tool Effectiveness table: → Retry column (sortable) | success-rate | high | Column header onClick toggles sort — functional; tooltip explains definition | keep — already actionable via sort. |
| Tool Effectiveness table: → Undo column (sortable) | success-rate | high | Column header onClick toggles sort — functional; tooltip explains definition | keep — already actionable via sort. |
| Tool Effectiveness table: 'top' badge | orientation | low | NONE — dead-end (decorative badge, no tooltip or action) | Add a tooltip explaining what 'top' means (highest effectiveness score among tools with ≥5 invocations). As a pure badge with no action path it is borderline vanity; the colored score cell in the same row already conveys the same signal. |
| Tool Effectiveness table: 'review' badge | success-rate | medium | NONE — dead-end (decorative badge, no tooltip or action) | Add a tooltip (or reuse EFFECTIVENESS_TOOLTIP already defined at line 112). Better: wire it as a button that navigates to the ErrorRetry view filtered to that tool, so 'review' actually starts the review. |

## files — **MIXED**

_Expose which files Claude touches most and where token waste comes from repeated reads, so the user can pin hot files in CLAUDE.md, split oversized tasks, or prune read-only reference loading._

Top fixes: - Add a CopyButton on every file path in both the Load-once Candidates table and the Per-session drilldown table that copies '@<path>' to clipboard — this is the single most-requested next action after reading the view (pin the file in CLAUDE.md) and currently requires the user to manually type the path.
- Wire the Compactions cell (Per-session drilldown, line 610) to navigate to the Context Health view for that session via onOpenSession — a non-zero compaction count is the clearest signal of eviction-driven re-reads and deserves a one-click drill-in, not a dead-end number.
- Wire ChartBar onClick events on both the Directory Hotspots and Read-only Files charts (or replace them with data tables) — both charts are fully non-interactive today: the user sees a ranked list they cannot drill into, filter by, or copy from.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| KPI — Files Touched (count) | orientation | low | NONE — dead-end | Make it a link/filter that pre-filters the Most Active Files table to the same set, or drop it — the table already shows the full list. |
| KPI — Total File Ops (count) | orientation | low | NONE — dead-end | Cut it or replace with 'Ops this week vs prior week' to give a trend the user can act on (rising = scope creep or churn). |
| KPI — Read-only Files (count) | context-health | medium | NONE — dead-end | Wire an onClick that scrolls to / highlights the Read-only Files chart below, or add a tooltip: 'These files are never edited — consider caching them with @path in CLAUDE.md'. |
| KPI — Top Directory (path) | workflow-hygiene | medium | NONE — dead-end | Wire onClick to scroll to / filter the Directory Hotspots chart to that directory, or show the op count alongside the path so the user can immediately judge whether the hotspot is expected. |
| Most Active Files table — sort toggle buttons (Total / Churn / Edits/Sess) | workflow-hygiene | high | onClick={() => setActiveSort('total'\|'churn'\|'edits-per-session')} — re-sorts table in place | keep — already actionable |
| Most Active Files table — File path column | workflow-hygiene | low | NONE — dead-end (title tooltip shows full path on hover only) | Add a CopyButton (copy path to clipboard) so the user can paste it directly into CLAUDE.md @path syntax. The path is the primary actionable artifact here. |
| Most Active Files table — Reads count | context-health | low | NONE — dead-end | No standalone fix needed if the Load-once Candidates section is present; otherwise add a threshold badge ('High' if reads > 10) linking to the re-read section. |
| Most Active Files table — Edits count | workflow-hygiene | low | NONE — dead-end | Combine with Writes into a single Mutations column; the split is statistical noise for most users and adds two columns of dead-end data. |
| Most Active Files table — Writes count | workflow-hygiene | low | NONE — dead-end | Merge with Edits into a Mutations column (see Edits fix above). |
| Most Active Files table — Churn count (Edits+Writes) | workflow-hygiene | medium | NONE — dead-end | Add a threshold badge or inline note when Churn > N: 'High churn — consider splitting this file or reviewing scope creep.' No drill-in needed if a recommendations link is wired. |
| Most Active Files table — Edits/Session ratio | workflow-hygiene | medium | NONE — dead-end | Add a tooltip: 'Ratio > 5 may indicate repeated rework — consider refactoring or narrowing task scope.' The ratio is meaningful but currently has no label or threshold guidance. |
| Most Active Files table — Total ops count | orientation | low | NONE — dead-end | Cut the column if Reads + Edits + Writes columns are kept; it is a derived sum the user can compute mentally and adds no actionable signal beyond the sort toggle. |
| Directory Hotspots bar chart — bar height / ops count per directory | workflow-hygiene | medium | NONE — dead-end (Victory ChartBar has no wired onClick/events prop) | Wire ChartBar events prop with an onClick that filters the Most Active Files table to the clicked directory. Alternatively add a data table below the chart showing dir path, file count, and op count with a copy-path affordance. |
| Load-once Candidates — header summary (~N tokens wasted, M files, K sessions) | context-health | high | NONE — dead-end | keep — the numbers are contextualised by the table immediately below; no extra affordance needed. Consider a direct link to CLAUDE.md editing if the waste exceeds a threshold. |
| Load-once Candidates table — File path column | context-health | high | NONE — dead-end (title tooltip only) | Add a CopyButton that copies '@path' (the CLAUDE.md pin syntax) to clipboard. This is the single highest-value missing affordance in the view — the user reads the table and must then manually type the path. |
| Load-once Candidates table — Sessions count | context-health | medium | NONE — dead-end | Informative enough alongside the table; low priority. Could link to a Sessions view pre-filtered to sessions that re-read this file if that filter exists. |
| Load-once Candidates table — Reads count (total across sessions) | context-health | low | NONE — dead-end | Informative supporting data; acceptable as-is given Est. Waste column drives the action. |
| Load-once Candidates table — Worst Session read count (max per session) | context-health | medium | NONE — dead-end | Wire a click that scrolls to the Per-session drilldown filtered to this file path, so the user can immediately see which session was worst. |
| Load-once Candidates table — Est. Waste tokens (with quality badge ~ or ?) | context-health | high | NONE — dead-end | keep — actionable in context; the fix suggestion in the card description ('pin in CLAUDE.md or paste once up-front') is sufficient. The ~ / ? quality badge tooltip is well-wired. |
| Per-session drilldown table — Session ID link | discovery | high | onClick → onOpen(sessionId) → navigateTo('sessions') + setFocusSessionId — drill-in to Sessions view | keep — already actionable |
| Per-session drilldown table — File path column | context-health | high | NONE — dead-end (title tooltip only) | Add a CopyButton for '@path' clipboard copy — same fix as the Load-once Candidates file path column. |
| Per-session drilldown table — Reads count (per session, highlighted) | context-health | medium | NONE — dead-end | Informative supporting data; the session drill-in link in the same row gives the follow-through action. Acceptable as-is. |
| Per-session drilldown table — First → Last time span | context-health | low | NONE — dead-end (tooltip shows full timestamps; no interaction) | Informative context for diagnosing eviction-driven re-reads; no action needed beyond what the Compactions column already provides. Acceptable as-is. |
| Per-session drilldown table — Est. Waste (with * fallback indicator) | context-health | medium | NONE — dead-end (title tooltip on * explains estimate source) | keep — actionable via session link in same row; the row-level CopyButton fix for the file path covers the residual gap. |
| Per-session drilldown table — Compactions count (colored when > 0) | context-health | high | NONE — dead-end | Wire onClick (when > 0) to navigate to the Context Health view pre-filtered to this session (same openSession pattern). The compaction count is highly actionable — a user seeing '17 compactions' should be one click away from the context health drilldown. |
| Read-only Files bar chart — bar height / read count per file | context-health | medium | NONE — dead-end (Victory ChartBar has no wired events prop) | Wire ChartBar events with an onClick that copies the file path with '@' prefix to clipboard, so the user can immediately pin the reference file in CLAUDE.md. Alternatively, replace with a simple data table (path + read count + CopyButton) — the chart form provides no ordering signal the user can act on that a sorted table doesn't. |

## Sessions — **MIXED**

_Let the user scan, search, and drill into individual Claude sessions to identify expensive, error-prone, or context-heavy sessions and label them for downstream pattern analysis._

Top fixes: - Wire the three high-signal warn-colored metric tiles (Errors, Compactions, Peak context) to concrete next actions: Errors tile links/filters to the Errors view for this session; Compactions tile links to the compaction-hot-sessions recommendation or the Timeline view; Peak context tile (>=50%) surfaces the bloated-claude-md or compaction rec. These are the highest-value dead-ends because the data is already colored to signal a problem but offers no path to resolve it.
- Make the session ID footer a CopyButton and add a copy affordance to every file-path row in the Top Files chart. The ID and paths are the primary keys for every other tool the user has (CLI, grep, editor), so a one-click copy turns purely decorative text into a workflow accelerator.
- Replace the undiscoverable keyboard-only tag system (g/b/0) with a visible click-to-cycle tag button in the Label column. The good/bad ground-truth signal feeds the Patterns view but is effectively invisible to any user who has not read the tooltip or docs.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Day-group header date + session count badge | discovery | low | NONE — dead-end | Make the date label a link or filter chip that narrows the list to that day, or cut the badge (count is obvious from the rows below). |
| Session start time (Time column) | orientation | low | NONE — dead-end | Cut or fold into the Session title column — it does not enable any action on its own. At minimum surface the full ISO timestamp in a tooltip so it can be cross-referenced with other tools. |
| Session title / session ID short label (Session column link button) | discovery | high | onClick (line 874) expands the session detail row in-place | keep — already actionable |
| Project short name (sub-label under session title) | discovery | medium | NONE — dead-end | Wire an onClick that sets searchQuery to the project name so the user can filter to all sessions in that project with one click. |
| Message count (Msgs column) | orientation | low | NONE — dead-end | Cut from the table row — it duplicates the sort-by-Messages button. If kept, use it to jump to the transcript (same expand action as the title click). |
| Session duration (Duration column) | orientation | low | NONE — dead-end | Cut from the table row — it duplicates sort-by-Duration. If kept, color-code long sessions (>1h) as a cost/context-health warning to make the datum actionable. |
| Good/Bad tag badge (Label column) | workflow-hygiene | medium | FALSE AFFORDANCE — badge is styled with color (green/red Label) but has no onClick; tagging only works via undiscoverable keyboard shortcut (g/b/0) while hovering/focusing the row | Add an onClick to the SessionTagBadge (or a small tag icon button in the Label column) that cycles good → bad → clear, so the action is discoverable without reading docs. |
| Expand/collapse chevron button | discovery | medium | onClick (line 912) expands/collapses session detail — duplicates the title click | keep — already actionable (though redundant with title click) |
| Session overview — Total tokens metric tile (with in/out/cache breakdown sub and optional cache-read badge) | cost | medium | NONE — dead-end | Add a link/button on the tile that navigates to the Usage/Timeline view filtered to this session, or surface a copy-to-clipboard for the session ID so the user can grep their own data. |
| Session overview — Estimated cost metric tile | cost | medium | NONE — dead-end | Same as Total tokens: link to Usage view filtered by session, or trigger a Recommendation if cost exceeds a threshold (e.g. wire to rec engine). |
| Session overview — model name (CardTitle right side) | cost | medium | NONE — dead-end | If model is an expensive/legacy model, show a warning label linking to the legacy-model-overpay recommendation. Clicking the model name could open the Recommendations panel. |
| Session overview — 'estimated pricing' annotation when hasUnknownModel | cost | low | NONE — dead-end | Link to docs or the model list so the user knows which model string to expect, or surface the raw model string for filing a report. |
| Session overview — Duration metric tile (with start→end clock sub) | orientation | low | NONE — dead-end | Cut or merge with the table row Duration column — pure informational duplicate. |
| Session overview — User turns metric tile | orientation | low | NONE — dead-end | Cut — not actionable without a threshold or comparison. Could be cut to reduce clutter. |
| Session overview — Tool calls metric tile (with unique-tools sub) | workflow-hygiene | medium | NONE — dead-end | Link to the Tools view filtered by this session, or add a 'View tool breakdown' action that scrolls to the Top Tools bar chart already in the expanded panel. |
| Session overview — Errors metric tile (warn-colored when >0, tool/API breakdown sub) | success-rate | high | NONE — dead-end | When errorCount > 0, add an onClick that scrolls/links to the Errors view filtered to this session, or expand an inline error list showing error messages within the detail panel. |
| Session overview — Compactions metric tile (warn-colored when >0, first-compaction-at sub) | context-health | high | NONE — dead-end | When compactionCount > 0, link to the compaction-hot-sessions recommendation or navigate to the Timeline view for this session to show the compaction timeline. |
| Session overview — Peak context % metric tile (warn-colored >=50%, token/200K sub) | context-health | high | NONE — dead-end | When peakContextPercent >= 50%, add an onClick or warning button that opens/highlights the bloated-claude-md or compaction recommendations. A color-coded gauge bar would make the threshold scannable. |
| Top tools bar chart rows (tool name + proportional bar + count) | workflow-hygiene | medium | NONE — dead-end | Add an onClick per tool row that opens the Tools view filtered to this session + tool, or adds the tool name to a search query. At minimum add a copy-to-clipboard on the tool name. |
| Top files bar chart rows (file path + proportional bar + count) | workflow-hygiene | medium | NONE — dead-end | Add a copy-to-clipboard button on each file path row. The full path is truncated (shortFilePath helper at line 88); copying the full path lets the user open it in their editor. |
| User message text (expanded detail — timestamp + message body, truncated at 300 chars) | discovery | medium | NONE — dead-end | Add 'Show more' expansion on truncated messages, and a copy-to-clipboard button per message so the user can re-use or search their own prompts. |
| Assistant transcript text blocks (SessionTranscriptPf, server-mode only) | discovery | medium | NONE — dead-end for text blocks; tool_use blocks show name only as a Label badge (no handler) | Add copy-to-clipboard per text block. For tool_use Label badges, make them clickable to scroll to the matching user message or highlight the tool in the Top Tools chart. |
| Show/Hide thinking toggle button (SessionTranscriptPf) | discovery | medium | onClick (line 198) toggles thinking panel visibility | keep — already actionable |
| Session ID (footer of expanded detail row, monospace small) | discovery | medium | NONE — dead-end | Wrap in a CopyButton so the user can paste the session ID into other tools (CLI, grep). The raw ID is useful but invisible without copy affordance. |
| Search input (filter by title/project/session ID) | discovery | high | onChange (line 759) filters the session list; onClear (line 760) resets | keep — already actionable |
| Sort buttons: Recency / Duration / Messages | discovery | medium | onClick (line 770) changes sort order | keep — already actionable, though adding a Cost sort option would directly serve the cost action domain. |
| Load more button (pagination) | discovery | medium | onClick (line 996) loads the next 20 sessions | keep — already actionable |
| Empty-search 'No sessions match' message | discovery | low | NONE — dead-end | Add a 'Clear search' button inline so the user does not have to scroll back to the toolbar to recover. |

## projects — **MIXED**

_Show how Claude usage is distributed across projects and worktrees so the user can identify which projects drive the most activity and drill into their sessions._

Top fixes: - Activity heatmap dots: add onClick per dot that calls onOpenSession for the busiest session on that date (or navigates to Sessions filtered to project+date) — this converts the most visually prominent expanded-panel element from pure decoration into the highest-value drill-in action in the view
- Full project path: wrap in a PF CopyButton — one-line change, lets the user cd or open the directory immediately; also make the branch badge on worktree rows a copy button for 'git checkout <branch>'
- Date range and standalone % label: cut both or replace the date range with 'last active N days ago' linked to the Timeline view filtered to this project — the raw first/last timestamps and the redundant percentage number are pure orientation with no attached action

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Project / repo name (collapsed header) | discovery | medium | onClick at :160 — toggles expand/collapse | keep — already actionable; add a secondary icon-button or right-click menu to navigate to Tokens/Cost view filtered to this project |
| Message count (header row, e.g. '142 msgs') | orientation | low | NONE — dead-end (swallowed by parent toggle button, no separate onClick) | Link to Token Usage or Cost Attribution view pre-filtered to this project, or add a tooltip citing the cost implication of message volume |
| Session count (header row, e.g. '7 sessions') | orientation | low | NONE — dead-end (swallowed by parent toggle button) | Navigate to Sessions view pre-filtered to this project on click, or surface a 'Filter sessions to this project' action in an expanded panel |
| Progress bar (share of total messages) | orientation | low | FALSE AFFORDANCE — cursor:pointer on parent button implies the bar itself is clickable, but it only expands the card | Cut the bar or replace with a spark-number that links to the Cost Attribution view filtered to this project; the raw share-of-messages figure has no action |
| Percentage label (e.g. '12.4%' bottom-right of header) | orientation | none | NONE — dead-end | Cut it (the progress bar already encodes the same value visually); if kept, make it a link to the Cost view filtered to this project |
| Date range (firstSeen – lastSeen, e.g. 'Apr 3 – Jun 1') | orientation | none | NONE — dead-end | Cut it or replace with 'last active N days ago' which surfaces project staleness — an actionable signal (abandon or revive the project) — and link to the Timeline view filtered to this project |
| Worktree count badge (e.g. '3 worktrees', RepoGroupRow only) | orientation | none | NONE — dead-end | Cut it; the worktree children are already visible on expand. If retained, make the count a button that expands the group. |
| Worktree branch label badge (e.g. 'feature/123-foo', nested LeafProjectRow) | workflow-hygiene | low | NONE — dead-end (PF Label has no onClick wired) | Make the branch badge a copy-to-clipboard button so the user can run 'git checkout <branch>' immediately; or add a 'git worktree remove' command suggestion when the worktree has been idle for >N days |
| Full project path (expanded panel, monospace block) | discovery | low | NONE — dead-end | Add a CopyButton (PF component) next to the path so the user can cd into the directory or open it in their editor without manual selection; cost is one import + one JSX wrapper |
| Activity heatmap dots (one dot per active day, colour encodes message density) | discovery | low | NONE — dead-end (tooltip fires on hover, but no click handler exists on the dots) | Add onClick to each dot that calls onOpenSession for the busiest session on that date, or navigates to the Sessions view pre-filtered to that project+date; this is the highest-leverage interaction in the expanded panel |
| Session row: start date + time (e.g. 'May 28 02:14') | discovery | high | onClick at :270 (onOpenSession wrapper button) — navigates to Sessions view and focuses that session; handler wired at App.tsx:515 | keep — already actionable |
| Session row: message count (e.g. '14 msgs') | cost | high | onClick at :270 (onOpenSession wrapper button) — navigates to Sessions view and focuses that session | keep — already actionable; consider also showing token count here since message count alone does not indicate cost |
| Session row: session ID prefix (8 hex chars) | discovery | high | onClick at :270 (onOpenSession wrapper button) — navigates to Sessions view and focuses that session; handler defined at App.tsx:253-259 | keep — already actionable; add a copy-ID button alongside for scripting use (e.g. claude --resume <id>) |
| 'Show all (N)' / 'Show top 10' toggle button | discovery | medium | onClick at :309 — toggles showAllSessions Set for this project key | keep — already actionable |
| Session row: falls back to plain div when onOpenSession is absent | discovery | none | NONE — dead-end (cursor is default, not pointer; no title tooltip) | The prop is always passed from App.tsx:515, so this path is unreachable in practice; add a runtime assertion or remove the fallback branch to eliminate the dead code |

## AutomationView — **MIXED**

_Give users a single home for all unattended SDK/CLI automation runs — their cost share, a searchable/sortable session list with per-session drill-down, and a chronological timeline strip — so they can diagnose spend, reliability, and behavior patterns in unattended workloads._

Top fixes: - Wire warn-colored metric tiles (Errors, Compactions, Peak context) in the per-session overview panel to their respective detail views (ErrorRetry, ContextHealth) scoped to the session ID — these are the highest-visibility dead-ends: red/orange warning styling with no navigation target is a false affordance and destroys user trust in the warnings.
- Make the Automation cost share bar chart clickable: automation segment navigates to Cost Attribution filtered to sdk-* sessions; add a CopyButton or sort-shortcut to the automation legend label ($X.XX, N%) so the most decision-relevant number on the card leads somewhere actionable.
- Add a CopyButton to the session ID footer in the expanded detail panel and make tag badge (good/bad) directly clickable — together these fix the two most frequently reached dead-ends in the session drill-down flow.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Total estimated cost (header) | cost | low | NONE — dead-end | Make the label a link that navigates to the Cost Attribution view filtered to automation sessions, or add a CopyButton so the dollar figure is at least copyable for budgeting. |
| Automation cost share stacked bar chart (automation segment) | cost | low | NONE — dead-end | Wire a click on the automation (blue) segment to open the Cost Attribution view pre-filtered to sdk-* sessions. The 'Other' segment click could navigate to Cost Attribution unfiltered. The bar is already aria-labeled with the dollar values — adding onClick costs nothing. |
| Automation cost share stacked bar chart (other/rest segment) | orientation | none | NONE — dead-end | Same as the automation segment fix above, or cut it entirely — the 'Other' segment tells the user nothing they can act on inside the Automation view. |
| Automation cost legend label ($X.XX, N%) | cost | medium | NONE — dead-end | Add a 'See sessions by cost' sort shortcut here (set sortKey to 'cost'), or a link to the Cost Attribution view filtered to automation. The percentage number is the most decision-relevant figure on the whole card. |
| Other cost legend label ($X.XX, N%) | orientation | none | NONE — dead-end | Cut it. The 'Other' figure is derivable from Total minus Automation and has no action the user can take from the Automation view. |
| Session list search input | discovery | high | onChange/_evt → setSearchQuery — interactive filter | keep — already actionable |
| Session list sort dropdown | discovery | high | onChange/_evt → setSortKey — interactive sort | keep — already actionable |
| Filter result count ('N of M sessions') | orientation | none | NONE — dead-end | Keep as context label (it is genuinely useful while filtering) but no separate fix needed — it only appears alongside an active search. |
| Day-group sticky header with session count badge | orientation | low | NONE — dead-end | Add a collapse/expand toggle to the day header so users can hide inactive days. Alternatively link the date to a calendar/timeline view pre-scoped to that day. |
| Session row — Time column (start time) | orientation | none | NONE — dead-end | Consider removing or merging with the session title row; it is readable context but adds no action beyond what the expand already gives. |
| Session row — Session title / name (expand button) | discovery | high | onClick → setExpandedId(expanded ? null : session.sessionId) — expands inline detail panel | keep — already actionable |
| Session row — Project sub-label | discovery | medium | FALSE AFFORDANCE — displayed inline beneath the session title link, appears as supplementary clickable text but has no handler | Wrap project name in a link/button that navigates to the Project Breakdown view filtered to that project, or adds it to the search query. It currently looks navigable but does nothing. |
| Session row — Msgs column (message count) | orientation | low | NONE — dead-end | Wire a click to set sort to 'messageCount' so a single click on any message count re-sorts by that dimension, matching the pattern of column-header sort links. |
| Session row — Duration column | speed | low | NONE — dead-end | Wire a click to set sort to 'duration'. Same cheap column-header sort pattern. |
| Session row — Label (good/bad tag badge) | workflow-hygiene | high | keyboard shortcut only — g/b/0 keys while row is focused/hovered; badge itself has no onClick | Add a direct click handler on the badge to cycle good → bad → clear so mobile/mouse users can tag without knowing the hotkey. The hotkey is undiscoverable on touch devices. |
| Session row — Expand chevron button | discovery | high | onClick → setExpandedId toggle — opens inline detail panel | keep — already actionable |
| Session overview panel — Total tokens metric tile | cost | medium | NONE — dead-end | Add a 'See token breakdown' link navigating to the Token Usage view pre-filtered to this session ID, or add a CopyButton on the value for budget tracking. |
| Session overview panel — Estimated cost metric tile | cost | medium | NONE — dead-end | Link to Cost Attribution view scoped to this session, or add a CopyButton. This is the most budget-relevant figure in the panel. |
| Session overview panel — Duration metric tile | speed | none | NONE — dead-end | Consider cut or merge into row; no action follows from duration alone at the session level. |
| Session overview panel — User turns metric tile | orientation | none | NONE — dead-end | Cut it from the overview panel — 'user turns' is not actionable for unattended/sdk runs where there are no interactive user turns. |
| Session overview panel — Tool calls metric tile | workflow-hygiene | medium | NONE — dead-end | Link to Tool Usage view scoped to this session, or at minimum make the tile expand/scroll down to the 'Top tools' breakdown already shown in the same panel. |
| Session overview panel — Errors metric tile | success-rate | high | NONE — dead-end (warn styling present but no handler) | Wire onClick to navigate to the Errors / Retry view filtered to this session ID. A red error count with nowhere to go is the clearest false-affordance in the panel. |
| Session overview panel — Compactions metric tile | context-health | high | NONE — dead-end (warn styling present but no handler) | Wire onClick to navigate to the Context Health view scoped to this session. Compaction events are the primary signal for context-window pressure — a warning with no path forward is a dead end. |
| Session overview panel — Peak context metric tile | context-health | high | NONE — dead-end (warn styling present but no handler) | Same as compactions: wire to Context Health view for this session. |
| Session overview panel — Top tools list (tool name + bar + count) | workflow-hygiene | medium | NONE — dead-end | Make tool names clickable to navigate to the Tool Usage view filtered to that tool name. The bar chart already looks interactive (styled rows with counts) but has no handler. |
| Session overview panel — Top files list (file path + bar + count) | workflow-hygiene | medium | NONE — dead-end | Make file paths clickable to navigate to the File Impact view filtered to that file. The bar chart rows are styled identically to the top tools list but equally dead-end. |
| User messages list (expanded detail — timestamped message texts) | orientation | none | NONE — dead-end | Keep as read-only context; no action expected on individual automation messages. Long messages are truncated at 300 chars — a 'show full' toggle would improve usability but is not an actionability gap. |
| Session ID footer (raw UUID text) | discovery | medium | NONE — dead-end | Add a CopyButton (or make the ID text itself a copy-on-click element) so users can paste the ID into the search bar or a terminal command. The ID is already used as a drill-in key everywhere else in the dashboard. |
| Show N more sessions (day-group expand button) | discovery | high | onClick → setExpandedDays mark day as expanded | keep — already actionable |
| Automation run timeline strip — individual run bar | discovery | high | onClick → onOpenSession(run.sessionId) — fires App.tsx:openSession which switches to Sessions view and focuses the session | keep — already actionable |
| Run bar tooltip (hover title: sessionId, entrypoint, start time, duration, entry count) | discovery | medium | HTML title attribute only — hover-only tooltip, not accessible on touch, not copyable | Promote the key fields (entrypoint, cost if available, entry count) to a persistent label below the bar strip, or replace the title with a PF Tooltip component so it works on touch and with keyboard focus. The session ID prefix should have a CopyButton. |
| Timeline window start timestamp | orientation | none | NONE — dead-end | Keep as axis label — it is necessary orientation context for the timeline strip, not a standalone data point requiring action. |
| Timeline total run count ('N runs') | orientation | none | NONE — dead-end | Keep as context summary — no standalone action needed. |
| Timeline window end timestamp | orientation | none | NONE — dead-end | Keep as axis label — same as start timestamp above. |

## Conversation Patterns — **MIXED**

_Surface per-session conversation shape metrics (turns, latency, velocity, thinking ratio) so users can identify sessions with unusual patterns and drill into them._

Top fixes: - Fix the false affordance on the Velocity column (src/components/ConversationPatterns.tsx:521-530): brand-blue coloring implies a link but no onClick exists. Wire it to onOpenSession(row.sessionId) or recolor it neutral — this is the highest-trust issue because it actively misleads users.
- Wire the latency histogram bars to filter the per-session table (src/components/ConversationPatterns.tsx:265-272): clicking a latency bucket should filter/highlight sessions in that band, turning the chart from a decorative histogram into a triage funnel for slow sessions (speed domain).
- Connect the four KPI summary tiles to the table (src/components/ConversationPatterns.tsx:219-234): each tile should either sort the per-session table by its corresponding metric (Avg Turns → sort by userTurns desc, Avg Code-block → sort by codeBlockMessages desc) or link to a related view, eliminating four orientation-only dead-ends in one pass.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Sessions Analyzed (KPI tile) | orientation | none | NONE — dead-end | Make the tile a link that opens the Sessions view (navigateTo('sessions')), or cut it — it duplicates a count available everywhere. |
| Avg Turns / Session (KPI tile) | orientation | none | NONE — dead-end | Add an onClick that sorts the per-session table by userTurns descending so the user immediately sees their wordiest sessions. |
| Total Questions Asked (KPI tile) | orientation | none | NONE — dead-end | Add an onClick that sorts the table by questionMessages descending, or cut the tile — question count alone has no behavioral implication without a drill path. |
| Avg Code-block Msgs / Session (KPI tile) | orientation | none | NONE — dead-end | Add an onClick that sorts the table by codeBlockMessages descending so the user finds the most code-heavy sessions. |
| Response Latency Distribution histogram (bar chart) | speed | low | ChartTooltip on hover (constrainToVisibleArea) — shows reply count per bucket; no drill-through on click | Wire an onClick on ChartBar data points (or a 'see sessions in this bucket' button below the chart) that filters the per-session table to sessions whose average latency falls in the clicked bucket — turns a histogram into a triage funnel. |
| Session ID cell (table) | discovery | high | onClick → openSession(sessionId) → navigateTo('sessions') + setFocusSessionId | keep — already actionable |
| User Turns (table column) | workflow-hygiene | medium | Column header is sortable via handleHeaderClick (line 354); cell value itself has no handler | keep — sort is sufficient; optionally highlight rows with anomalously high turn counts (> 2 SD above mean) to make outliers self-evident without a click. |
| Avg Len (chars) (table column) | context-health | low | Column header is sortable via handleHeaderClick (line 368); cell value has no handler | High avg message length drives CLAUDE.md bloat and context pressure. Add a recommendation badge (e.g. 'Avg prompt >2 000 chars — consider shorter prompts') when the value exceeds a threshold, or link to the Context Health view for sessions above threshold. |
| Questions (table column) | orientation | none | Column header is sortable via handleHeaderClick (line 381); cell value has no handler | Question count has no actionable target in the current app. Either cut the column or map it to workflow-hygiene insight (e.g. 'high question ratio = under-specified prompts') and link to a recommendation. |
| Code Blocks (table column) | orientation | none | Column header is sortable via handleHeaderClick (line 394); cell value has no handler | Cut or repurpose: code-block count per session is decorative without context. If kept, badge sessions with zero code-block messages as 'non-coding session' to help users understand token spend distribution. |
| Thinking % (table column) | cost | medium | Column header is sortable via handleHeaderClick (line 407); cell value has no handler | High thinking ratio drives token cost. Add a threshold badge (e.g. flag rows > 50%) and wire the cell or badge to open the session in the Sessions view, or surface it as a cost detector in the recommendations engine. |
| Entries (table column — denominator) | orientation | none | NONE — dead-end | Cut this column from the visible table. It is a raw denominator shown only to explain Thinking %; a tooltip on the Thinking % header ('N entries total') conveys the same information without consuming a column. |
| Velocity (msg/hr) (table column) | speed | medium | FALSE AFFORDANCE — styled in brand blue (var(--pf-t--global--color--brand--default)) suggesting a link, but no onClick handler on the cell | Either wire the cell to openSession(row.sessionId) (cheapest fix, removes false affordance) or change the cell color to a neutral/informational color. High-velocity sessions may indicate fast context exhaustion and warrant investigation. |
| Duration (table column — denominator) | orientation | none | NONE — dead-end | Cut or convert to a tooltip on the Velocity header. Duration shown without linking to the session timeline adds no actionable path. |
| Load more button | discovery | low | onClick → increments visibleCount by PAGE_SIZE (20) | keep — pagination is necessary; consider replacing with virtual scrolling for very large session lists to avoid repeated clicks. |

## Agents & Skill Usage — **MIXED**

_Help the user understand and improve how they use subagents, skills, and MCP servers so they can reduce cost, eliminate ineffective invocations, and fix failing workflows._

Top fixes: - Wire 'Continuations blocked' tile and Effectiveness 'Failures' column to drill into sessions (via onOpenSession + agent-type filter) — these are the highest-signal safety/reliability signals on the view and are currently complete dead-ends.
- Fix the false affordance on MCP Server name column (brand-blue color with no handler): either add a CopyButton for the server ID or open the MCP config path, and apply the same 'expand on click' pattern to all three truncated-text columns (Top Tools, AFK Summary, Top follow-ups, Setting value) so clipped data is reachable.
- Add USD cost estimates inline to the Skills and MCP Output Tokens columns (reuse formatUSD already imported), and annotate the No-op rate and hook-overhead tiles with threshold colors so high-cost / high-overhead agents surface their action signal without requiring cross-view calculation.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Total Agent Invocations (KPI tile) | orientation | low | NONE — dead-end | Make the tile a link/button that filters the Sessions view to only sessions containing agent spawns (reuse onOpenSession pattern or add a navigate-to-sessions-filter callback). |
| Distinct Agent Types (KPI tile) | orientation | none | NONE — dead-end | Cut or merge into the Top Subagent Types chart header as a subtitle; the chart already shows all distinct types. |
| Total Skill Invocations (KPI tile) | orientation | low | NONE — dead-end | Make it a link to the Top Skills table section (anchor scroll), or add a drill-in to Sessions filtered by skill invocations. |
| Sessions Using Agents (KPI tile) | cost | medium | NONE — dead-end | Wire an onClick that calls the parent's session-filter callback with AGENT_SPAWN_TOOLS filter — high-cost agent sessions are prime review candidates. |
| Top Subagent Types bar chart (bar per agent type, showing invocation count) | workflow-hygiene | medium | NONE — dead-end (tooltip only, no drill-in on bar click) | Add onClick to ChartBar data items that filters sessions by that subagent_type, using onOpenSession or a new onFilterByAgent prop, so users can inspect sessions driven by a specific agent. |
| Attribution mode badge on Top Subagent Types chart (native attribution / heuristic) | orientation | low | NONE — dead-end | Add a tooltip or popover explaining what 'heuristic' vs 'native attribution' means and what the user can do to get better data (e.g. upgrade Claude Code version). |
| Top Skills table — Skill name column | workflow-hygiene | medium | NONE — dead-end | Add an onClick that filters sessions by skill name, or opens a popover showing the skill's definition path (~/.claude/skills/<name>) with a copy-path button. |
| Top Skills table — Invocations column | orientation | low | NONE — dead-end | Keep as-is or make sortable; low-invocation skills are candidates for removal (link to workflow-hygiene recommendation). |
| Top Skills table — Sessions column | discovery | low | NONE — dead-end | Make it a button that filters the Sessions view to sessions that invoked this skill. |
| Top Skills table — Output Tokens column | cost | medium | NONE — dead-end | High-token skills should link into the cost view or show an inline cost estimate in USD via formatUSD so cost consequence is immediately visible. |
| Attribution mode badge on Top Skills table (native attribution / heuristic) | orientation | low | NONE — dead-end | Same as chart badge: add tooltip explaining how to unlock native attribution. |
| MCP Usage table — Server name column (brand-colored text) | workflow-hygiene | medium | FALSE AFFORDANCE — styled brand-blue (clickable color) but no onClick/href handler | Wire onClick to open ~/.claude.json MCP config path for this server, or copy the server ID to clipboard; the brand color must not be applied without a real action. |
| MCP Usage table — Top Tools column (truncated list of tool:count) | workflow-hygiene | medium | NONE — dead-end (content is clipped with no expand affordance) | Add a popover/tooltip that shows the full ranked tool list; otherwise the user cannot see what the most-used tools beyond 5 are. |
| MCP Usage table — Invocations column | orientation | low | NONE — dead-end | Keep as-is; context provided by adjacent columns. |
| MCP Usage table — Sessions column | discovery | low | NONE — dead-end | Make it a button that filters the Sessions view to sessions using this MCP server. |
| MCP Usage table — Output Tokens column | cost | medium | NONE — dead-end | Render USD estimate inline (same fix as skills Output Tokens column) so cost consequence of MCP tool usage is visible without a cross-view calculation. |
| Runtime Telemetry — Active turns measured (mini KPI tile) | orientation | none | NONE — dead-end | Cut or merge into section subtitle; pure denominator, no user action possible. |
| Runtime Telemetry — Median turn latency (mini KPI tile) | speed | medium | NONE — dead-end | Add a threshold annotation (e.g. amber if > 30s) and a tooltip suggesting where to look for slow hooks or network calls; a bare number gives no action signal. |
| Runtime Telemetry — p95 turn latency / active (mini KPI tile) | speed | medium | NONE — dead-end | Same threshold annotation fix as Median; surface a drill-in to the slowest sessions (those with turns near p95) so the user knows which sessions to investigate. |
| Runtime Telemetry — Max turn latency / active (mini KPI tile) | speed | medium | NONE — dead-end | Add onClick to open the session that contained the max-latency turn via onOpenSession. |
| Runtime Telemetry — Idle/AFK turns count (mini KPI tile, accent-bordered when > 0) | speed | medium | NONE — dead-end | Add onClick to navigate to sessions with idle turns; users seeing many idle turns may want to enable scheduled wakeups or review session gaps. |
| Runtime Telemetry — Max idle turn (mini KPI tile) | orientation | low | NONE — dead-end | Same drill-in to session with max idle turn; or cut if session link for idle count tile is added. |
| Runtime Telemetry — Stop-hook events count (mini KPI tile) | workflow-hygiene | low | NONE — dead-end | Combine with hook overhead tiles and add a link to ~/.claude/settings.json hooks config so the user can audit what hooks are running. |
| Runtime Telemetry — Mean hook overhead (mini KPI tile with ~7% caveat footnote) | speed | medium | NONE — dead-end | Add a threshold: if mean overhead > 1s, show warning color and link to settings.json hooks so the user can remove slow hooks. |
| Runtime Telemetry — Max hook overhead (mini KPI tile with ~7% caveat footnote) | speed | medium | NONE — dead-end | Same fix as mean hook overhead tile. |
| Runtime Telemetry — Continuations blocked (mini KPI tile, accent-bordered when > 0) | safety | high | NONE — dead-end | This is the highest-signal tile in the section — a blocked continuation means a hook vetoed the agent. Add onClick that drills into sessions where preventedContinuation=true, and add a CopyButton for the hook command string so the user can inspect/fix it immediately. |
| AFK & Scheduled Activity table — Timestamp column | orientation | none | NONE — dead-end | Keep as-is; pure context. |
| AFK & Scheduled Activity table — Kind badge (scheduled / AFK) | workflow-hygiene | low | NONE — dead-end | Add onClick that filters the table to show only scheduled or only AFK rows, so users can audit scheduled tasks separately from idle recaps. |
| AFK & Scheduled Activity table — Session column (clickable button when onOpenSession is provided) | discovery | high | onClick → onOpenSession(e.sessionId) — opens session detail | keep — already actionable |
| AFK & Scheduled Activity table — Summary column (truncated text, no expand) | discovery | medium | NONE — dead-end (text is clipped with no expand affordance) | Wrap in a PF Tooltip or use a popover-on-click so the full AFK/scheduled recap text is readable; truncated summaries are useless for understanding what the agent actually did. |
| Effectiveness table — Agent name column | discovery | medium | NONE — dead-end | Add onClick to filter sessions by agent type. |
| Effectiveness table — Runs column (sortable header) | workflow-hygiene | medium | onClick → handleEffSort('runs') — re-sorts table | keep — already actionable |
| Effectiveness table — Median time column | speed | medium | NONE — dead-end | Add threshold coloring (e.g. red > 5 min) and make the value link to sessions for this agent type sorted by duration. |
| Effectiveness table — Artifact rate column (sortable header) | workflow-hygiene | medium | onClick → handleEffSort('produceArtifactRate') — re-sorts table | keep — already actionable |
| Effectiveness table — No-op rate column (sortable header, accent-colored when >= 50%) | workflow-hygiene | high | onClick → handleEffSort('noOpRate') — re-sorts table | Sort affordance is good; add a row-level action: when noOpRate >= 50%, show an inline 'Review' button that opens the sessions for that agent so the user can decide whether to retire it. |
| Effectiveness table — Mean $/run column (sortable header) | cost | high | onClick → handleEffSort('meanCostUsd') — re-sorts table | keep — already actionable; consider adding a cost-per-month projection column or budget alert threshold annotation. |
| Effectiveness table — Top follow-ups column (truncated text) | workflow-hygiene | medium | NONE — dead-end (text is clipped with no expand affordance) | Add PF Tooltip on hover showing full ranked follow-up list, since this column is the evidence base for the Suggested agent tasks section. |
| Effectiveness table — Failures column (sortable header, accent-colored when > 0) | success-rate | high | onClick → handleEffSort('failureModes') — re-sorts table | Sort affordance is good; add a row-level drill-in to show failing sessions for that agent type so the user can examine the error transcripts. |
| Effectiveness table — Sessions column | discovery | medium | NONE — dead-end | Make it a button that calls onOpenSession with a filter for sessions that ran this agent type. |
| Suggested agent tasks — workflow suggestion text items (list of heuristic messages) | workflow-hygiene | medium | NONE — dead-end | Each suggestion names a specific agentType; add a drill-in button per item that navigates to the Sessions view filtered to that agent type, and where applicable (failure suggestion) add a CopyButton for the suggested fix command. |
| Suggested agent tasks — heuristic badge (per-item Label) | orientation | none | NONE — dead-end | Cut the per-item badge — the card title already carries the heuristic label; redundant badging adds noise without information. |
| Agent Settings Events table — Timestamp column | orientation | none | NONE — dead-end | Keep as-is; pure context. |
| Agent Settings Events table — Session column (clickable button when onOpenSession provided) | discovery | high | onClick → onOpenSession(evt.sessionId) — opens session detail | keep — already actionable |
| Agent Settings Events table — Setting column (name = value, name brand-colored) | workflow-hygiene | medium | NONE — dead-end (long values are ellipsis-truncated with no expand) | Add PF Tooltip on the Td showing the full untruncated setting value, so the user can read the complete setting that was applied and decide whether to change it in settings.json. |

## insights — **MIXED**

_Surface the /insights skill's narrative report and aggregated metrics so the user can understand workflow patterns and act on specific improvement suggestions._

Top fixes: - Wire friction categories to the Recommendations view: each 'Where Things Go Wrong' card should carry a 'See recommendations' button/link that navigates to recommendations filtered to that friction type — this is the single highest-leverage dead-end because friction findings are explicitly prescriptive yet currently have no exit path.
- Add CopyButton to every 'At a Glance' sub-block (especially Quick wins and Ambitious workflows) — these are the most immediately actionable prose items and require only a one-component wrapper already present in the codebase (CopyButton from InsightsReport.tsx:39-61).
- Make categorical chart bars drillable: clicking a bar in 'Top Tools' should open the Tools view filtered to that tool, and clicking a bar in 'Tool Errors' should open the Errors view filtered by that error category — charts are the widest dead-end surface in the Metrics section.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Regenerate Insights button | workflow-hygiene | high | onClick fires regenerateInsights(), polls /api/insights/status, calls onRegenerated when done | keep — already actionable |
| Last generated timestamp | orientation | low | NONE — dead-end | keep as-is; it is cheap context that supports the adjacent Regenerate button decision — acceptable dead-end |
| Header stat tiles (e.g. Total Sessions, Total Duration, Messages, etc.) | orientation | low | NONE — dead-end | Add onClick to each tile that navigates to the Sessions or Stats view filtered to the relevant dimension (e.g. clicking 'Total Sessions' opens sessions list; clicking 'Cost' opens cost view) |
| At a Glance — What's working | workflow-hygiene | low | NONE — dead-end | Add a CopyButton so the user can paste the finding into Claude Code as a follow-up question; or link to the Recommendations view |
| At a Glance — What's hindering you | workflow-hygiene | medium | NONE — dead-end | Add a CopyButton so the user can paste the friction text into Claude Code, or surface a link to the Recommendations view that often addresses the same friction |
| At a Glance — Quick wins to try | workflow-hygiene | high | NONE — dead-end | Add a CopyButton per quick-win item so the user can paste it directly into Claude Code; this is the cheapest fix and directly enables action on the highest-value block |
| At a Glance — Ambitious workflows | workflow-hygiene | medium | NONE — dead-end | Add a CopyButton so the user can paste the ambitious workflow prompt into Claude Code |
| Project areas (name + session count + description) | discovery | medium | NONE — dead-end | Make each project area card clickable to navigate to the Projects view pre-filtered to that project name |
| How You Use Claude Code — usage narrative paragraphs | orientation | none | NONE — dead-end | Cut or collapse behind a 'Read more' toggle; purely narrative, no concrete action flows from it |
| How You Use Claude Code — key insight callout | workflow-hygiene | medium | NONE — dead-end | Add a CopyButton so the user can paste the key insight as a Claude Code prompt |
| Big wins list (title + description) | orientation | none | NONE — dead-end | Cut or collapse; no concrete behavior change follows from reading a big-win card with no link to the session that produced it |
| Friction categories (title + description + examples) | success-rate | high | NONE — dead-end | Add a 'See recommendations' link/button per friction category that navigates to the Recommendations view; that view already surfaces fixes for the same friction types. This is the highest-value dead-end in the view. |
| Horizon cards — title + possible + tip | workflow-hygiene | medium | NONE — dead-end | keep — the copyable prompt immediately below already captures the action; the prose is supporting context for the CopyButton |
| Horizon cards — copyable prompt (CopyButton) | workflow-hygiene | high | CopyButton wired: onClick copies card.copyablePrompt to clipboard | keep — already actionable |
| Fun ending (headline + detail) | orientation | none | NONE — dead-end | Cut it; purely decorative vanity copy, does not drive any action |
| Session summaries search input | discovery | medium | onChange filters the summaries list; onClear resets | keep — already actionable |
| Session summary rows (sessionId link + brief summary text) | discovery | high | SessionIdLinkPf onClick fires onOpen(sessionId) which sets focusSessionId and navigates to sessions view | keep — already actionable |
| Session summaries pagination | discovery | medium | onSetPage updates virtual window offset | keep — already actionable |
| Categorical bar charts (What You Wanted, Top Tools, Languages, Session Types, What Helped, Outcomes, Satisfaction, Friction Types, Tool Errors) | workflow-hygiene | medium | NONE — dead-end | Make bars clickable: 'Top Tools' bar click filters to the Tools view by that tool; 'Tool Errors' bar click filters to the Errors view by that error category; 'Friction Types' bar click links to the Recommendations view. At minimum add a 'See in [view]' footer link on each chart card. |
| Response time distribution chart (bucket bars) | speed | low | NONE — dead-end | Add a link to the Sessions view sorted by response time, or surface a recommendation if the median is above a threshold (e.g. >30s median suggests context bloat) |
| Response time median and average stats | speed | low | NONE — dead-end | Show a threshold badge (e.g. 'Slow — check context health') when median exceeds 60s, linking to the Context view |
| Multi-clauding: Overlap Events count | orientation | low | NONE — dead-end | Link to the Sessions view filtered to sessions whose time ranges overlapped; or note whether high overlap correlates with higher error rate |
| Multi-clauding: Sessions Involved count | orientation | low | NONE — dead-end | Same as Overlap Events: link to Sessions view filtered to the overlapping sessions |
| Multi-clauding: Message Share % | context-health | low | NONE — dead-end | Add a threshold-based advisory note (e.g. '>50% overlap may fragment context') and link to the Context view |
| Time-of-day histogram bars (Morning/Afternoon/Evening/Night) | orientation | none | NONE — dead-end (timezone selector is the only wired control in this card) | Cut or collapse; peak usage hour is informational with no actionable next step in this app |
| Timezone selector (in time-of-day card) | orientation | medium | onSelect updates tzOffset which re-buckets hour bars | keep — already actionable as a display control (enables the user to interpret hours in their own timezone) |

## Cost Attribution — **MIXED**

_Show where estimated spend goes across tools, projects, and sessions so users can identify the most expensive behaviors and redirect or reduce that spend._

Top fixes: - Wire all three 'Cost by Project' table rows (project name, sessions, cost cells) to navigateTo('projects') with a project filter — the full path is already in the fullProjectByShort map (line 108-112) and navigateTo is available via onOpenSession's parent scope in App.tsx:546-553. One onClick per Tr unlocks 3 dead-end data points at once.
- Add a Victory events onClick to the Cost by Tool ChartBar (line 279) that calls navigateTo('tools') filtered to the clicked tool name. The tooltip already surfaces call count and session count — giving the chart a click path converts the most prominent visual element in the view from decoration to a genuine drill-in.
- Replace the 'Sessions Analyzed' KPI tile (pure orientation, no action possible) with a 'Avg Cost per Session' or 'Sessions above $X' threshold tile that gives the user a benchmark they can act on (e.g. reduce expensive sessions below a target).

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Total Est. Cost KPI tile | orientation | low | NONE — dead-end | Add a click handler that navigates to the 'tokens' view (TokenUsage), which already has per-session drill-in. The tile value is a sum; the tokens view provides the per-model breakdown needed to act on it. |
| Most Expensive Tool KPI tile (tool name + cost) | cost | medium | NONE — dead-end | On click, scroll-to or highlight the corresponding bar in the 'Cost by Tool' chart below, or navigate to the 'tools' view pre-filtered to that tool. The tool name alone is useful signal only if the user can then act on tool usage. |
| Most Expensive Project KPI tile (project name + cost) | cost | medium | NONE — dead-end | On click, navigate to the 'projects' view pre-filtered to that project. The full path is already in the tooltip; the missing piece is a navigation hook. |
| Sessions Analyzed KPI tile | orientation | none | NONE — dead-end | Cut it or replace with a more actionable KPI (e.g. 'Cost per session' or 'Sessions over budget threshold') that gives the user a benchmark to act against. |
| Cost by Tool bar chart — each bar (tool name, cost, call count, sessions in tooltip) | cost | medium | NONE — dead-end (tooltip shows data on hover, no click navigation) | Add an onClick handler to ChartBar (Victory supports events prop) that navigates to the 'tools' view filtered by the clicked tool name. The call count and session count in the tooltip are already enough to motivate action but the user has no path to get there from the chart. |
| Cost by Project table — Rank column (#) | orientation | none | NONE — dead-end | Cut it; rank is implicit from row order in a sorted table and wastes column space. |
| Cost by Project table — Project column (shortened path, full path in title tooltip) | cost | medium | NONE — dead-end | Make each project row clickable: navigate to the 'projects' view passing the project path. The full path is already available (fullProjectByShort map at line 108-112); only the onClick + navigateTo call is missing. |
| Cost by Project table — Sessions column (count) | discovery | low | NONE — dead-end | Make the count a link that navigates to the 'projects' view filtered to that project, or co-locate it with the clickable project name fix above so the whole row is a drill-in. |
| Cost by Project table — Cost column (USD value, bold) | cost | low | NONE — dead-end | Covered by making the entire row clickable (see Project column fix). No additional work needed once the row has an onClick. |
| Top Expensive Sessions table — Rank column (#) | orientation | none | NONE — dead-end | Cut it; same reason as the project-table rank column. |
| Top Expensive Sessions table — Session label (time + title, clickable when onOpenSession provided) | discovery | high | onClick → onOpenSession(sessionId) → navigates to sessions view with focus | keep — already actionable |
| Top Expensive Sessions table — Tool Calls column (count) | cost | low | NONE — dead-end | Add a tooltip explaining what a high call count implies (cost driver), or link to the 'tools' view. At minimum, the value should be contextualized (e.g. 'above average' badge) so the user knows whether to act. |
| Top Expensive Sessions table — Top Tool column (tool name or '-') | cost | medium | NONE — dead-end | Make it a link to the 'tools' view filtered by that tool name, so the user can see all sessions where that tool dominated cost and decide whether to reduce or replace it. |
| Top Expensive Sessions table — Cost column (USD value, bold) | cost | low | NONE — dead-end | Covered by the session label click (the whole row value is visible once you drill into the session). Alternatively make the entire row clickable instead of just the session label cell. |

## Evaluator — **FAIL**

_Surface four aggregated quality signals (per-task cost, tool-call latency, retry-storm rate, reread-loop fraction) to help the user diagnose whether their agent workflow is efficient and reliable._

Top fixes: - Pass onNavigate (already in App.tsx) and onOpenSession into EvaluatorLandingPf — every other view gets these but the Evaluator is wired without them (App.tsx:463). Without them no tile can drill down anywhere.
- Wire all four Tile headers (or a 'Details' button on each) to their corresponding specialist views: Per-task cost → 'cost', Tool-call latency → 'agents', Retry-storm → 'errors', Reread-loop → 'files'. This turns the Evaluator from a pure read-out into a triage dashboard.
- Promote the top retry-storm tool and top reread-offender file into the tile bodies (e.g. a compact third StatCardPf showing 'top tool: Bash' or 'top file: CLAUDE.md') so the user can act without leaving the view at all.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Median cost / task | cost | low | NONE — dead-end | Add an onNavigate prop (already wired in App.tsx for other views). Wire the tile or a 'See breakdown' link to navigate to the 'cost' view, passing a filter so the user lands on the sessions that dominate p95 cost. |
| p95 cost / task | cost | low | NONE — dead-end | Same as Median cost / task: link to the 'cost' view. The p95 outliers are exactly the sessions worth drilling into; surface the top-N session IDs driving the p95 and wire them with onOpenSession. |
| Per-task cost caption (task count + sessions with stop hooks) | orientation | none | NONE — dead-end | Keep as context text but add a note that tasks without stop-hook boundaries are excluded; link to the 'errors' or 'automation' view if hookCount is 0 to guide the user to activate stop hooks. |
| p50 (median) tool-call latency | speed | low | NONE — dead-end | Wire to the 'agents' view (AgentSkill) which already shows per-skill latency distributions. The evaluator number is the aggregate; the agents view lets the user identify which skill or tool is responsible. |
| p95 tool-call latency | speed | low | NONE — dead-end | Same as p50 latency: link to the 'agents' view. The p95 is the number that reveals pathological outlier turns worth fixing; surfacing the top-N slowest sessions/turns would make it immediately actionable. |
| Tool-call latency caption (active turn count, idle turn count, threshold) | orientation | none | NONE — dead-end | Keep as explanatory context. Optionally add an idle-turn count link to the 'automation' view which shows away summaries / scheduled fires that generate those idle turns. |
| Retry storms (absolute count) | success-rate | medium | NONE — dead-end | Wire to the 'errors' view (ErrorRetry) which already shows retry groups by tool and session. Pass the retry group list; the user needs to see WHICH tool is storming to fix it. This is the cheapest high-value fix in the view. |
| Retry storms per 1k tool calls (rate) | success-rate | medium | NONE — dead-end | Same link as absolute storm count: navigate to 'errors'. The rate normalizes for volume but without knowing which tool to fix, it is vanity. |
| Retry-storm caption (tool calls count, affected sessions count, native api_error backoff events + total backoff ms) | success-rate | low | NONE — dead-end | The native api_error backoff summary (retryEvents, totalBackoffMs) is buried in caption text. Promote retryPressure.retryEvents to a third StatCardPf tile inside the grid, and wire all three to the 'errors' view. |
| Sessions affected by reread loops (percentage) | context-health | medium | NONE — dead-end | Wire to the 'files' (FileImpact) view which already renders the per-(session, file) reread table. Pass a flag to pre-filter on offenders. User can then pin the worst files to CLAUDE.md. |
| Estimated tokens wasted by rereads | context-health | medium | NONE — dead-end | Same as sessions-affected: link to 'files' view. The token waste number is the cost justification for adding a file to CLAUDE.md; without the file names it is unactionable. The cheapest fix is any nav to FileImpact. |
| Reread caption (threshold, session count, filesAffected count, totalEstimatedTokenWaste) | context-health | low | NONE — dead-end | filesAffected is the most actionable number here — the user wants to know which files to pin, not how many. Move filesAffected to a third StatCardPf tile and wire it to the 'files' view. The caption text alone is inert. |

## Session Timeline — **FAIL**

_Replay the chronological message-by-message record of a single session so the user can diagnose what happened during that session (tool calls, errors, thinking turns, delays)._

> **Correction (2026-07-08):** #2346 / PR #2353 shipped the suggested
> selected-session affordance. Timeline now renders **Open in Sessions →**
> beside the picker, with a copy fallback when navigation is unavailable. The
> deprecated/unused-prop claims below describe the 2026-06-03 snapshot.

Top fixes: - Wire row onClick on every entry: the hover highlight (onMouseEnter/Leave) already implies interactivity — add a click handler to expand the truncated summary inline or in a slide-out panel. This is the single biggest gap: users see highlighted rows and click nothing.
- Make error-kind rows (tool_result isError=true) link directly to the Errors & Retries view via navigateTo('errors') — error entries are the highest-signal diagnostic data in the timeline and are currently a dead-end.
- Activate the already-wired but deprecated onOpenSession prop on the session picker: add an 'Open in Sessions view' button so the user can navigate from the timeline's session context to full token/cost metadata for that same session.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Session picker (FormSelect) | discovery | medium | onChange → setUserSelectedId (local state only, stays on this view) | Add an 'Open in Sessions view' button or link next to the picker that calls onOpenSession(selectedId) — the prop exists and is wired in App.tsx:255-258 but is never called by this component (marked @deprecated at line 103) |
| Total entries count | orientation | none | NONE — dead-end | Cut it or fold into the session picker label (entries count already appears there at line 254); a standalone number with no drill-in adds nothing |
| Session duration | orientation | low | NONE — dead-end | Make it a link to the Sessions view (onOpenSession) so the user can see token cost and full session metadata alongside the duration |
| Per-kind entry count breakdown (user/assistant/tool_use/tool_result/thinking/other) | workflow-hygiene | low | NONE — dead-end | Add click-to-filter: clicking a kind label filters the visible entries list to that kind; without filtering, counts are orientation-only |
| Claude Code version badge (cc: <version>) | orientation | none | NONE — dead-end | Cut it or link to the 'recommendations' view filtered to legacy-model-overpay/version detectors; knowing the version number alone prompts no action |
| Entrypoint badge (via: cli / sdk-py / sdk-cli) with orange/green color coding | workflow-hygiene | medium | NONE — dead-end (color signals unattended but there is no action wired) | Make the orange (sdk-*) badge link to the Automation view where unattended session patterns are analyzed; a colored badge with no destination is a false affordance hint |
| Git branch badge (branch: <name>) | orientation | none | NONE — dead-end | Cut it unless a copy-to-clipboard button is added (cheap: wrap in a CopyButton so the user can run 'git checkout <branch>') |
| Service tier badge (tier: default / priority / batch) | cost | low | NONE — dead-end | Link to the Recommendations view; 'priority' tier is exactly what the priority-tier-spend detector fires on — surfacing that connection here makes the badge actionable |
| Row timestamp (HH:MM:SS) | orientation | none | NONE — dead-end | Keep as context; consider showing full ISO on hover via title attribute so the user can correlate with external logs — low cost, small gain |
| Row time-delta (+Ns / +Nm / +Nh between consecutive entries) | speed | medium | NONE — dead-end | Highlight rows whose delta exceeds a threshold (e.g. >30 s) in amber to flag latency spikes; without that signal the deltas are just numbers the user must mentally scan |
| Entry kind dot + label (user / assistant / tool_use / tool_result / thinking / other / error) | success-rate | medium | NONE — dead-end (onMouseEnter/Leave at 373-379 adds hover highlight but no click action) | Wire an onClick on error-kind rows to jump to the Errors & Retries view (onOpenSession / navigateTo('errors')); error entries are the most actionable signal in the list and currently go nowhere |
| Tool name label on tool_use rows (bold green) | workflow-hygiene | medium | NONE — dead-end | Make tool name clickable to jump to the Tools view filtered to that tool; tool names are exactly what the Tools view analyzes for effectiveness |
| Entry summary text (truncated, ellipsis overflow) | discovery | high | FALSE AFFORDANCE — the entire row has an onMouseEnter hover highlight (cursor-change implied) but clicking the row does nothing | Wire onClick on the row div to expand the summary inline (toggle full text) or open a modal with the full entry; the hover highlight strongly implies clickability that never arrives — highest priority fix in the view |
| Truncation notice ('… N more entries truncated') | discovery | low | NONE — dead-end | Add a 'Load more' / 'Export full session' affordance; the user is silently dropped at 2000 rows with no way to see the rest |

## stats — **FAIL**

_Give the user a bird's-eye picture of how much they have used Claude Code over time, across projects and hours of the week._

Top fixes: - Wire the Project Distribution chart bars to navigateTo('projects') / navigateTo('cost') on click — project bars are the highest-leverage dead-end because project message count directly maps to cost and already has a drill-in view (ProjectBreakdown) that accepts a project filter. One Victory onClick handler is all that is needed.
- Wire all four KPI tiles to their natural destination views (Total Sessions → 'sessions', Avg Msgs/Session → 'conversation', Avg Session Duration → 'context' or 'patterns') by accepting an onNavigate prop in UsageStatsPf and passing navigateTo from App.tsx (line 522-524 already owns the sessions and entries props — just extend the interface).
- Replace the plain HTML title tooltip on heatmap cells with an interactive element (a clickable cell or a Popover) that shows the raw count and a 'View sessions' link targeting navigateTo('sessions'); this converts the most visually prominent element of the view from pure vanity into a discovery affordance.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Total Sessions KPI tile | orientation | low | NONE — dead-end | Add onClick={() => navigateTo('sessions')} to the Card so tapping the tile drills into the Sessions list, filtered to the same date window. |
| User Messages KPI tile | orientation | none | NONE — dead-end | Cut it or replace it with a metric that connects to an action (e.g. messages containing errors → navigateTo('errors'), or high-message sessions → navigateTo('sessions') filtered by messageCount). |
| Avg Msgs/Session KPI tile | orientation | none | NONE — dead-end | Wire onClick to navigateTo('conversation') (ConversationPatterns already analyses turn depth and has actionable next steps) so the number leads somewhere. |
| Avg Session Duration KPI tile | workflow-hygiene | low | NONE — dead-end | Wire onClick to navigateTo('context') (ContextHealth surfaces long-context sessions and compaction opportunities) or navigateTo('patterns') (SessionPatterns breaks down duration distribution). |
| Activity Over Time area chart (daily message count) | orientation | low | NONE — dead-end | Add brush/click on a bar/date segment that calls navigateTo('sessions') with a date-range filter, or at minimum add a link-button 'View sessions on this day' via ChartBar events. Victory charts expose datum.x in onClick events. |
| Project Distribution horizontal bar chart (message count per project) | cost | medium | NONE — dead-end | Wire ChartBar events: onClick on a bar should call navigateTo('projects') with that project pre-selected (ProjectBreakdown already accepts onActiveProjectChange). High-message projects are the primary cost driver, so a single click to the cost or projects view makes the bar actionable. |
| Activity Heatmap — individual heat cell (hour x day of week count) | workflow-hygiene | low | NONE — dead-end | Add onClick to each cell that calls navigateTo('sessions') pre-filtered by day-of-week + hour range, so peak hours surface which sessions are driving the concentration. Without a filter API in SessionList this is a low-cost tooltip-only improvement: replace the plain title= with a richer tooltip showing the count and a 'View sessions' link. |
| Activity Heatmap — day-of-week row label (Sun/Mon/...) | orientation | none | NONE — dead-end | No fix needed for the label itself; fix belongs on the cells it annotates (see heat-cell row above). |

## activity — **FAIL**

_Show the user when and where they work so they can understand their usage patterns and identify their most active projects — but the view currently stops at display and drives zero follow-on actions._

> **Correction (2026-07-08):** #1623 wired ProjectActivity's aggregate
> day/project drilldowns. #2346 / PR #2353 then confirmed that the component
> should not accept `onOpenSession` and removed that unused prop. Suggestions
> below to add or reuse `onOpenSession` are superseded; the original review is
> preserved as a 2026-06-03 point-in-time record.

Top fixes: - Wire onNavigate + onOpenSession props into ProjectActivityPf (App.tsx:561 must pass them). Every actionable fix in this view — calendar cell drill-in, project row navigation, top-project KPI — is blocked because the component interface declares no callbacks at all. Adding the props costs ~5 lines in the interface + App.tsx call site.
- Make the Active projects table project-name cell (line 448) a clickable button that calls navigateTo('projects') with that project pre-selected. This is the single highest-signal data point on the view (momentum rank) and it currently dead-ends. ProjectBreakdown already has this wiring pattern (onOpenSession) to copy from.
- Add per-cell onClick to the 90-day calendar heatmap (line 287–305): clicking a day navigates to Sessions filtered to that date. This converts the most visually prominent element from a read-only heatmap into a drill-down entry point, turning every non-zero cell into a discovery action.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Active days (90d) — KPI tile | orientation | low | NONE — dead-end | Make the tile a button: onClick navigates to the sessions view pre-filtered to the last 90 days (pass onNavigate prop + filter state) |
| Sessions in window (90d) — KPI tile | orientation | none | NONE — dead-end | Same as above: clicking should open Sessions filtered to last 90 days, or cut this tile if Active days already covers it |
| Peak hour — KPI tile | workflow-hygiene | low | NONE — dead-end | Add a tooltip or inline note explaining what the user should do with this (e.g. 'Schedule focused blocks here'); or link to a calendar/scheduling affordance |
| Top project — KPI tile | discovery | medium | NONE — dead-end (has title= tooltip with full path, but no click) | Wire onClick to navigate to Projects view filtered to that project (pass onNavigate + project filter prop) |
| Calendar heatmap cell (per-day session count, 90 days) | discovery | medium | NONE — dead-end (title tooltip shows 'YYYY-MM-DD: N sessions' but no click) | Add onClick per cell: navigate to Sessions view filtered to sessions on that specific date |
| Calendar heatmap — overall chart (Last 90 days card) | workflow-hygiene | low | NONE — dead-end | Keep as orientation context only if cells become clickable; otherwise the pattern itself (streaks vs. gaps) has no action path — at minimum add a recommendation link when streak breaks exceed N days |
| Hour-of-day bar chart (Victory/PF ChartBar, 24 hourly totals) | workflow-hygiene | low | NONE — dead-end | Add Victory events/onClick on bars to navigate to Sessions filtered by that hour; cheapest alternative is a static callout below the chart naming the top-3 active hours |
| Hour × weekday heatmap cell (all-time, per weekday+hour count) | workflow-hygiene | low | NONE — dead-end (title tooltip shows 'Day HH:00: N sessions' but no click) | Add onClick per cell: navigate to Sessions view filtered to sessions in that weekday+hour slot |
| Active projects table — Project name column (projectShort) | discovery | high | NONE — dead-end (title tooltip with full path, no click) | Highest-leverage fix: add onClick (or render as a button/link) to navigate to Projects view filtered to that project — identical pattern to ProjectBreakdown's onOpenSession wiring |
| Active projects table — Sessions column (sessionCount) | discovery | medium | NONE — dead-end | Clicking should open Sessions view filtered to that project; lower priority than the project name cell but same fix path |
| Active projects table — Weeks active column (weeksActive) | orientation | none | NONE — dead-end | Pure derived stat with no clear action; consider cutting it or folding it into a tooltip on sessions/week |
| Active projects table — First seen column (firstSeen) | orientation | none | NONE — dead-end | Cut or collapse into a tooltip; a project start date alone drives no action |
| Active projects table — Last seen column (lastSeen) | workflow-hygiene | medium | NONE — dead-end | Flag stale projects (last seen > 30 days ago) with a badge; clicking the date navigates to the most recent session for that project via onOpenSession |
| Active projects table — Sessions / week column (sessionsPerWeek) | orientation | low | NONE — dead-end | This is the sort key and most meaningful metric; make it the click target to open that project's detail, or add a sparkline. Low priority fix compared to the project-name cell |
| Active projects table — Rank column (#) | orientation | none | NONE — dead-end | Keep as a visual aid (no action needed); not worth cutting |

## config-hygiene — **MIXED**

_Surface installed-but-unused resources (skills, subagents, MCP servers, plugins) and settings.json validation errors so the user can prune dead config weight and fix broken configuration before it silently misbehaves._

Top fixes: - Add a CopyButton to the 'python3 -m json.tool <filePath>' command hint and to the filePath element itself — these are the two highest-friction copy-paste moments for the settings-fix workflow, and both are zero-logic one-liner additions.
- Wire resourceId and the Lifetime count to navigate to the Sessions view filtered to that resource: the single biggest gap is that 'unused' and 'last used Nd ago' are tombstones the user cannot verify or drill into without leaving the dashboard manually.
- Wire the FindingCard line/column to a CopyButton emitting 'filePath:line' (VS Code jump-to-line format) — this collapses the 3-step 'find file → open → scroll' into a single paste action for every settings error.

| Data point | Domain | Actionability | Affordance | Fix |
|---|---|---|---|---|
| Total unused count badge (header) | workflow-hygiene | low | NONE — dead-end | Make the badge a clickable anchor that scrolls to the first resource group section, or cut it — the same number is derivable by scanning the groups below. |
| Settings file path (code element showing health.filePath) | workflow-hygiene | medium | NONE — dead-end | Add a CopyButton next to the path so the user can paste it directly into a terminal editor command (e.g. 'code ~/.claude/settings.json'). |
| Settings valid/invalid badge ('Valid' or 'N errors') | workflow-hygiene | medium | NONE — dead-end | When invalid, wire the badge as a drill-in link (or scroll anchor) to the first error FindingCard below it; the badge alone tells the user nothing new the error cards don't already say. |
| Settings warnings count badge ('N warning(s)') | workflow-hygiene | low | NONE — dead-end | Same as the error badge — either scroll-anchor to warnings or collapse into the error badge as 'N errors, M warnings'; standalone warning count is redundant when the finding cards are already listed below. |
| Settings 'pass' card (valid JSON confirmation text) | orientation | none | NONE — dead-end | Cut it. A confirmation that nothing is broken gives the user nothing to act on. The absence of error/warning cards already communicates this. |
| SettingsFindingCard: severity badge (error/warning label) | workflow-hygiene | low | NONE — dead-end | Keep as context — it earns its place by colouring urgency — but add a CopyButton on the whole card so the user can copy the path+message to clipboard before opening the file. |
| SettingsFindingCard: JSON path of finding (dotted path like 'permissions.deny[3]') | workflow-hygiene | medium | NONE — dead-end | Add a CopyButton so the user can paste the path directly into a jq/editor query. A clickable copy is the single cheapest lift here. |
| SettingsFindingCard: line/column number | workflow-hygiene | high | NONE — dead-end | Wrap in a CopyButton that copies 'filePath:line' (VS Code jump-to-line format). Users can then 'code -g ~/.claude/settings.json:14' to open at the exact error. This is the highest-value single-click fix in the settings panel. |
| SettingsFindingCard: human-readable error/warning message | workflow-hygiene | high | NONE — dead-end | Keep — the message already tells the user what to do. No handler needed; context genuinely enables the adjacent fix action. Pass as-is. |
| SettingsFindingCard: code excerpt (offending JSON line) | workflow-hygiene | high | NONE — dead-end | Keep — the excerpt is load-bearing context. Add a CopyButton on the <pre> so the user can paste it into a JSON linter/AI chat without re-typing. One-liner addition. |
| Syntax error fix command hint ('python3 -m json.tool <path>') | workflow-hygiene | high | NONE — dead-end | Keep the text; add a CopyButton that copies the exact command string so the user can paste it directly into a terminal. The command is already correct — copy-to-clipboard is the only missing step. |
| Resource group heading + per-type unused count (e.g. 'Skills — 3 unused') | workflow-hygiene | low | NONE — dead-end | The count is redundant with the FindingCard list directly below. Cut the count chip, or keep only the heading as an anchor target for the top-level badge link (see fix for data point 1). |
| FindingCard: resourceId (skill/agent/MCP server/plugin name, bold code) | workflow-hygiene | high | NONE — dead-end | Add a CopyButton so the user can copy the id to paste into 'claude skill remove <id>' or the MCP config editor. Optionally wire a link to the Sessions view filtered to that resource so the user can verify the 'never used' claim by browsing raw session data. |
| FindingCard: scope badge ('Global' or 'Project: ...') | workflow-hygiene | medium | NONE — dead-end | For project-scoped findings, wire the badge to navigate to that project in the Sessions/Projects view so the user can cross-check usage in context. For global scope the badge is pure orientation — keep as label, no change needed. |
| FindingCard: last-seen text ('Never used' / 'Last used Nd ago, no invocations in 30d') | workflow-hygiene | medium | NONE — dead-end | When lastSeen is non-null, link 'Last used Nd ago' to the Sessions view filtered to that session (the finding carries lastSeen epoch ms — resolve to sessionId and pass as a query param to onNavigate). This converts a tombstone fact into a drill-in path. |
| FindingCard: hedge warning (orange text — 'retained history shorter than 30-day threshold') | workflow-hygiene | low | NONE — dead-end | Keep as honesty signal — it is context that prevents a false-positive action (pruning a resource that might be used but just not in retained history). No handler needed; it earns its place. |
| FindingCard: Lifetime invocation count (headline number, right column) | workflow-hygiene | medium | NONE — dead-end | Wire a click on the count to navigate to the Sessions view filtered to sessions where that resource was invoked. A count > 0 with 0 window invocations means 'was used before but not recently' — the drill-in confirms whether the user ever depended on it before pruning. |
